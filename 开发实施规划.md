# 通用合同管理系统开发实施规划

> 版本：V1.0
>
> 适用范围：一期可商用 MVP 的开发实施
>
> 说明：一期、二期、三期是已经确定的产品分期。本文件中的“阶段”是开发顺序，不重新拆分产品范围。

## 1. 产品分期不变

### 1.1 一期：可商用 MVP

- 合同全生命周期：创建、编辑、版本、审批、签署、生效、履行、完成、终止、作废。
- 合同类型、子类型、模板、字段字典和类型扩展字段。
- 合同台账、工作台、合同详情、附件和导出。
- 交易对方外部引用、我方主体引用和关键时点信息。
- AI 合同解析和法律风险审查，必须人工确认。
- 外部审批适配，开发阶段使用 Mock。
- 统一文件服务适配，开发阶段使用 Mock 或测试环境。
- 角色、数据范围、合同类型、模板、字段和合同级授权。
- 操作审计、AI 任务审计、审批同步审计、文件访问审计和导出审计。

### 1.2 二期：增强能力

- 外部交易对方主数据系统深度对接。
- 金额分级审批。
- 多部门、多组织叠加数据隔离。
- 部分收付、逾期自动标记和提醒。
- 多币种折算。
- 主体增强去重和合并。
- 统计报表增强、数据库签署快照等已定义的二期能力。

### 1.3 三期：扩展能力

- 电子签章。
- 归档借阅。
- 智能续约、条款比对和更多 AI 能力。

二期、三期只做边界预留，不在一期实现，不因为预留字段把一期流程做复杂。

## 2. 一期开发总路线

```text
开发基线确认
  -> 工程骨架与数据库迁移
  -> 合同核心后端闭环
  -> 文件/审批/AI Mock 接入
  -> 前端核心页面
  -> 权限、审计和商用安全补齐
  -> 外部服务联调
  -> 一期验收与上线准备
```

这里的先后是工程实施顺序，不代表产品分期变化。

## 3. 开发前基线

### 3.1 必须采用的权威资料

- 业务需求：`合同管理系统需求设计.md`
- 功能评审：`合同管理系统需求思维导图.md`
- 字段和表结构：`D:\project\hr-contract\数据库表\合同管理.docx`
- AI 规则：`AI专家体需求设计.md`
- 文件集成：需求文档第 2.3 节“文件服务与合同中心区”
- 开发实施：本文件

归档目录和旧数据库 Markdown 不作为开发依据。

### 3.2 开发前必须冻结的内容

以下内容现作为一期开发基线定稿，不再逐项重复确认：

1. 一期启用六类高频合同：劳动合同、劳务协议、顾问协议、采购合同、销售合同、服务合同；对应大类为用工与人员类、交易合作类。租赁、借款、担保、保密、知识产权、许可及自定义类型保留配置能力，暂不启用业务流程。
2. 每个启用类型先提供“通用”子类型，子类型通过类型配置和动态表单扩展，不在代码中写死；业务量明确后只新增配置和 Schema，不改通用合同主流程。
3. 合同来源统一为：模板起草、文件导入、手工录入、外部系统同步。数据库枚举和需求文档按此四项实现。
4. `party_source_type` 只表示交易对方来源系统；`party_role` 只表示签约角色（甲方、乙方、出租方等）；客户、供应商、合作方、员工统一使用独立的合同业务关系角色字段 `business_relation_type`。如当前 DOCX 缺少该字段，迁移基线增加它，并同步回 DOCX。
5. 交易对方、HR/组织、我方主体、文件服务、审批系统和 AI Provider 均通过 Adapter 接入；一期全部提供 Mock，正式接口到位后只替换 Adapter，不改合同领域模型和页面 API。
6. 合同业务状态、审批状态、AI 解析状态和 AI 审查状态分开保存；审批、AI、文件任务均异步可重试，所有写操作具备幂等和审计。
7. 权限采用“角色菜单/动作权限 + HR 组织数据范围 + 合同类型权限 + 字段权限 + 合同级授权”的交集规则；合同类型及子类型是实际授权单位，大类仅为导航分组。
8. 本地第一切片使用 SQLite，不依赖尚未提供的云数据库信息；后期目标数据库确定后，再按当前 `D:\project\hr-contract\数据库表\合同管理.docx` 生成对应迁移脚本并验证数据迁移，不读取历史 Markdown 表结构。第一切片先落地合同核心相关表和公共表，不为了未来类型提前复制业务表。
9. 开发顺序固定为：数据库/工程基线 → 合同核心后端闭环 → Mock 适配 → 前端闭环页面 → 商用加固 → 外部联调；前后端在同一业务切片内并行，不等待外部系统。

## 4. 技术结构

```text
前端 Web
   -> 合同系统 API
       -> 合同领域服务
       -> 权限与数据范围服务
       -> 文件适配层
       -> 审批适配层
       -> AI Provider 适配层
       -> 异步任务与补偿机制
       -> 审计服务
       -> 数据库 / 文件服务
```

### 4.1 业务系统负责

- 合同主数据、正文版本和类型扩展。
- 合同状态及状态转换校验。
- 权限、数据范围和敏感字段处理。
- 文件与合同的关联、版本和业务分类。
- AI 任务、结果版本、人工确认和审计。
- 外部审批实例关联、同步和补偿。

### 4.2 外部系统负责

- HR/组织系统负责员工、组织、岗位和数据范围；外部交易对方主数据系统负责客户、供应商、合作方等交易对方主数据。
- 文件服务负责合同中心区的物理存储、预览、下载和短期链接。
- 审批系统负责审批流程、节点和审批人。
- AI Provider 负责模型调用、工作流执行和结构化推理结果。

页面和业务服务不得直接依赖任何外部系统的返回结构。

## 5. 开发实施步骤

### 步骤一：数据库和工程基线

产出：

- 最终数据库迁移脚本。
- 初始化字典和角色数据。
- 后端工程骨架、配置管理、日志、异常和统一响应。
- 前端工程骨架、路由、登录态和权限指令基础。
- 本地开发环境和测试数据库。

要求：主键、逻辑删除、审计字段、枚举类型、索引和无物理外键规则全部按权威文档执行。

### 步骤二：合同核心后端闭环

第一条可验收主线：

```text
创建合同
  -> 选择合同类型/子类型
  -> 选择模板或导入文件
  -> 填写通用字段和类型扩展字段
  -> 保存草稿
  -> 查询台账
  -> 查看详情
  -> 编辑并生成正文版本
  -> 作废或提交审批
```

本步骤必须完成后端权限、乐观锁、状态校验、版本记录和操作审计。前端按钮显示不能代替后端校验。

### 步骤三：文件和审批 Mock

- 文件 Mock：申请上传、完成确认、预览链接、下载链接、删除/归档、哈希和大小校验。
- 审批 Mock：提交、查询、通过、驳回、撤回、重复回调、回调乱序、超时和补偿查询。
- 合同提交审批后保持业务状态为草稿，单独更新 `approval_status` 并锁定关键字段。
- 待签署撤回回草稿，原审批实例作废，重新提交生成新审批实例，历史不可覆盖。

### 步骤四：AI Provider Mock 和 AI 专家体

AI 专家体不是重新定义需求，而是依据现有 `AI专家体需求设计.md` 落地：

1. 先冻结解析和审查 JSON Schema。
2. 定义统一 AI Provider 内部接口。
3. 用 Mock 返回成功、低置信度、Schema 错误、超时和失败结果。
4. 完成 AI 任务、重试、幂等、版本绑定、人工确认和审计。
5. 准备脱敏合同评测集和人工标准答案。
6. 再配置 Dify 私有化工作流并接入 `dify` Provider。
7. 对比 Mock、Dify 和人工结果，达到验收标准后才进入真实联调。

AI 不审批、不签署、不改变合同状态、不自动确认业务字段。

### 步骤五：前端一期页面

按业务闭环实现：

1. 合同中心工作台。
2. 合同台账和筛选。
3. 合同创建和草稿编辑。
4. 合同详情及正文/版本、条款/履行、收付款、AI/审批、附件/变更、操作记录页签。
5. AI 解析和审查结果确认。
6. 审批状态和待办工作台。
7. 签署生效、履行、作废、终止和变更操作。

前端只消费合同系统 API；外部文件、审批和 AI 的原始响应不得直接暴露给页面。

### 步骤六：商用加固和联调

- 敏感字段加密、脱敏和日志防泄露。
- 密钥密文保存，禁止进入前端和普通日志。
- 文件上传病毒扫描、类型/大小限制和短期链接。
- 关键写操作幂等、乐观锁和重复请求处理。
- AI、审批和文件任务超时、重试、死信和人工补偿。
- 全链路审计和敏感操作告警。
- 权限越权、导出越权、附件越权和跨组织访问测试。
- 数据备份、恢复、归档和保留策略验证。
- 性能、并发、长文本、异常外部服务和灾备演练。

## 6. 统一适配契约

### 6.1 文件 Provider

```text
requestUpload(context) -> upload_ticket
confirmUpload(upload_ticket) -> file_ref
createAttachment(contract_id, file_ref, attachment_meta) -> attachment
getPreviewLink(attachment_id) -> temporary_link
getDownloadLink(attachment_id) -> temporary_link
archiveAttachment(attachment_id, reason) -> result
```

系统内部只使用 `file_ref` 和附件 ID，不保存长期公开 URL。

### 6.2 Approval Provider

```text
submitApproval(contract_snapshot, idempotency_key) -> approval_instance
queryApproval(approval_instance_id) -> approval_status
withdrawApproval(approval_instance_id, reason) -> result
forceTransfer(approval_instance_id, target_user, reason) -> result
syncApprovalEvent(event_id, payload) -> result
```

审批事件必须按外部实例 ID 和事件 ID 幂等，允许回调乱序，并支持主动查询补偿。

### 6.3 AI Provider

```text
submitExtract(task_context, idempotency_key) -> ai_task
submitReview(task_context, idempotency_key) -> ai_task
queryTask(provider_task_id) -> task_status
getTaskResult(provider_task_id) -> provider_result
cancelTask(provider_task_id) -> result
testConnection(config) -> connection_result
```

`task_context` 必须包含合同 ID、正文版本 ID、合同类型、任务类型、审查视角和 Schema 版本。Provider 返回结果先经过合同系统 Schema 校验，再落库；解析结果和审查结果均不得直接覆盖人工确认数据。

## 7. 一期验收主线

### 7.1 正常流程

```text
新建草稿
 -> 生成或导入正文
 -> AI 解析并人工确认
 -> AI 法律审查并由法务确认
 -> 提交外部审批
 -> 审批通过进入待签署
 -> 上传签署文件并确认生效
 -> 履行事项和收付款计划
 -> 完成/终止
```

### 7.2 必测异常

- 重复提交合同、重复上传文件、重复发起 AI 任务。
- AI 超时、Schema 错误、低置信度、结果过期和人工重发。
- 审批重复回调、乱序回调、审批人变更和强制转交。
- 待签署撤回、审批驳回、合同作废和履行中终止。
- 多人同时编辑、版本冲突和审批期间修改关键字段。
- 无权限查看合同、敏感字段、附件、AI 结果和导出文件。
- 外部系统不可用时合同保存、历史查询和任务补偿。

## 8. 当前立即执行项

1. 整理并确认 `合同管理.docx` 的最终表清单和版本号。
2. 输出一期合同类型、角色权限和状态转换矩阵。
3. 输出文件、审批和 AI Provider 的接口契约文档。
4. 确定技术栈、数据库版本、认证方式和部署方式。
5. 创建 Mock 和测试用例，再开始后端工程骨架。

完成以上五项后，正式进入一期开发；不需要重新讨论一期、二期、三期的产品边界。

## 9. 统一技术与工程设计

本章合并原拆分方案的有效内容，作为当前唯一技术实施入口；原文件已移入 `归档/设计过程/`，只用于追溯。

### 9.1 产品整体边界

一期包含合同分类和类型扩展、模板、合同创建与导入、正文版本、交易对方引用、我方主体、生命周期、外部审批适配、AI 解析和审查、履行收付款、变更补充协议、附件、工作台、台账、详情、权限和审计。

一期不建设客户/供应商主数据页面、文件服务器、审批引擎、AI 自动审批/签署/确认、知识图谱，以及二期统计增强、多组织叠加和三期电子签章等能力。

产品叫“通用合同管理系统”；“合同 AI 专家体”是系统内 AI 子能力，不是独立产品或一级导航；Dify 只是一期 AI Provider 的候选实现。

### 9.2 页面职责

```text
合同中心：工作台 / 合同台账 / 履行与收付款 / 合同详情
系统管理：分类 / 模板 / 我方主体 / 外部连接 / 权限与审计
```

工作台只做指标和待处理事项，台账负责合同查询和筛选，创建页负责新建和草稿，详情负责单份合同完整业务上下文。交易对方只做统一选择和引用，不建设客户页或供应商页；AI、审批、签署、变更和附件由详情页签或流程入口承载。

### 9.3 统一业务模型

合同由分类版本、我方主体/角色、交易对方关系、正文版本、履行收付款、AI 任务、审批关联、文件关联和审计追溯组成。客户、供应商、合作方等都是交易对方在具体合同中的关系角色；来源系统和合同关系是两个维度。

```text
合同业务状态：草稿 → 待签署 → 履行中 → 已完成
草稿/待签署 → 作废；履行中 → 已终止；待签署撤回 → 草稿
审批、AI 解析、AI 审查和履行状态独立保存，不占合同业务状态节点。
```

### 9.4 外部系统

外部依赖包括 HR/组织、外部交易对方主数据、我方主体/法人、统一文件服务、统一审批和 AI Provider。客户与供应商可能来自同一个或多个外部系统，合同系统内部统一通过交易对方 Adapter 接入。

所有外部依赖先使用 Mock，正式接口到位后只替换 Adapter。接口契约必须包含鉴权、幂等、超时、重试、错误码、状态映射、回调签名、审计和补偿。

### 9.5 AI 专家体

```text
合同系统 Java → AI Provider 适配层 → Mock / Dify / 直接模型 API / 其他平台
```

Skill 作为解析和审查工作流中的规则/工具能力，不建设独立 Skill 平台；知识库一期需要，用于法律法规、公司制度、审查规则和标准条款，并保存来源、版本、生效日期和适用范围；知识图谱一期不做。

AI 结果必须绑定合同、正文版本、任务、Schema 和 Provider/工作流版本。Schema 错误、超时、低置信度和无原文依据必须进入失败或待人工确认，AI 不改变合同状态、不审批、不签署、不自动确认业务字段。

### 9.6 技术栈与工程边界

```text
Java 17 + Spring Boot 3.x + Spring MVC + MyBatis-Plus
本地 SQLite；目标数据库待客户确定后迁移至云 MySQL 8.x 兼容数据库
公司标准消息队列/任务调度 + Flyway + OpenAPI 3
Vue 3 + TypeScript；模块化单体
```

后端按 API、application、domain、infrastructure、integration、job、test 分层；前端按 pages、modules、api、permissions 组织。优先复用公司认证、权限、组织、日志、消息和部署基础设施。

### 9.7 第一开发切片

```text
选择合同类型
→ 选择交易对方及合同关系
→ 填写基础和扩展字段
→ 保存草稿
→ 台账查询
→ 进入详情
→ 再次编辑
→ 查看操作记录
```

第一切片同时完成前后端、权限、乐观锁、审计、异常和测试。首批内部 API 为合同类型、动态表单 Schema、交易对方查询、合同创建、台账、详情、草稿编辑、作废和操作日志查询。

### 9.8 商用底线

- 密钥密文保存，不进入前端和普通日志；合同原文、身份证号、薪资和完整 AI 输入不得写入普通日志。
- 文件预览/下载先校验合同权限，再申请短期链接；生产、测试、本地使用独立数据、文件空间和凭证。
- 前端隐藏按钮不能替代后端权限；每次查询和写操作重新校验数据范围、类型、字段、合同授权、状态和版本。
- API、任务、数据库、文件和外部调用必须可监控、可告警、可审计，并具备备份恢复、任务补偿、故障降级和回滚方案。

## 10. 当前唯一开发入口

1. 以当前 `D:\project\hr-contract\数据库表\合同管理.docx` 形成结构清单；当前本地使用 SQLite，待目标数据库信息确定后再生成对应 Flyway 迁移基线，不读取历史 Markdown 表结构。
2. 创建第一切片的领域模型、DTO、Mapper、Service、Controller 和 Mock Adapter。
3. 同时实现台账、创建草稿、详情和审计页面及 API 联调。
4. 再接入正文版本、文件 Mock、AI Mock、审批 Mock、签署履行和变更。

本文件是技术、接口、页面切片和工程实施的唯一方案入口；原拆分方案已归档，仅用于追溯。

## 11. 一期设计最终定稿

本章是一期开发的完整执行清单。除外部系统正式接口参数和部署环境参数需要在联调时替换外，一期不再新增产品设计确认项。

### 11.1 一期业务模块

| 模块 | 一期实现 | 负责位置 |
|---|---|---|
| 合同分类 | 大类、类型、通用子类型、启停用、类型扩展字段 | 系统管理/合同分类配置 |
| 合同创建 | 模板起草、文件导入、手工录入、外部同步入口 | 合同创建页 |
| 合同核心 | 草稿、编辑、版本、状态、我方主体、交易对方关系 | 合同领域服务 |
| 合同台账 | 查询、组合筛选、排序、导出入口、权限过滤 | 合同台账 |
| 合同详情 | 概览、正文版本、条款履行、收付款、智能与审批、附件变更、操作记录 | 合同详情 |
| AI | 合同解析、法律风险审查、任务状态、人工确认 | 合同详情智能页签 |
| 审批 | 提交、审批状态同步、撤回、驳回、转交、审批记录 | 审批工作台/详情页签 |
| 履行收付款 | 履行事项、付款计划、付款记录、到期提醒 | 合同详情/履行台账 |
| 变更与附件 | 变更协议、补充协议、附件关联、文件审计 | 合同详情 |
| 权限审计 | 角色动作、组织范围、类型权限、字段权限、合同级授权、操作审计 | 系统管理/详情记录 |

客户、供应商、合作方和员工不建设独立主数据页面，统一通过交易对方选择和引用完成。

### 11.2 合同类型与通用数据规则

- 一期启用：劳动合同、劳务协议、顾问协议、采购合同、销售合同、服务合同；每类提供“通用”子类型。
- 合同大类只负责导航分组；合同类型和子类型负责配置、权限和后端校验。
- 交易方向、协议性质、我方角色、签约角色和业务关系角色分别建模，不合并成一个枚举。
- 合同来源固定为：`TEMPLATE` 模板起草、`IMPORT` 文件导入、`MANUAL` 手工录入、`EXTERNAL_SYNC` 外部系统同步。
- `party_source_type` 表示来源系统；`party_role` 表示甲方/乙方等签约角色；`business_relation_type` 表示客户/供应商/合作方/员工等合同关系。
- 补充协议和变更协议必须关联原合同，不作为独立合同类型。
- 数据库表、字段、索引和约束全部按当前 `D:\project\hr-contract\数据库表\合同管理.docx` 生成；未来类型不复制新的业务主表。

### 11.3 状态与关键校验

```text
合同业务状态：草稿 -> 待签署 -> 履行中 -> 已完成
草稿/待签署 -> 作废
履行中 -> 已终止
待签署撤回 -> 草稿
```

- `approval_status` 独立表示审批状态，审批中不改变合同业务状态；提交审批后锁定关键字段。
- AI 解析和 AI 审查各自维护任务状态：未发起、处理中、待人工确认、已完成、失败；不改变合同业务状态。
- 创建草稿必须校验启用类型、启用主体、交易对方可用性、必要字段和版本乐观锁。
- 提交审批必须校验正文或原始文件、关键字段完整性、合同权限和幂等键。
- 作废、撤回、驳回、终止、完成、审批提交和人工确认均记录操作人、原因、前后状态和关联版本。

### 11.4 一期内部 API

```text
GET  /api/contract-types
GET  /api/contract-types/{id}/form-schema
GET  /api/trade-parties
GET  /api/our-subjects
POST /api/contracts
PUT  /api/contracts/{id}/draft
GET  /api/contracts
GET  /api/contracts/{id}
GET  /api/contracts/{id}/versions
GET  /api/contracts/{id}/operation-logs
POST /api/contracts/{id}/void
POST /api/contracts/{id}/submit-approval
GET  /api/approval/tasks
POST /api/ai/tasks/extract
POST /api/ai/tasks/review
GET  /api/ai/tasks/{id}
POST /api/ai/tasks/{id}/confirm
```

所有写接口统一返回业务结果、错误码、追踪 ID 和版本信息；写接口必须支持幂等，查询接口统一执行权限和数据范围过滤。前端只消费合同系统 DTO，不直接依赖外部系统响应。

### 11.5 页面信息架构

```text
合同中心
  工作台：指标和待处理事项入口
  合同台账：完整查询、筛选、导出
  履行与收付款：履行和收付款查询
  合同创建：类型选择、交易对方、基础字段、正文/模板、保存草稿
  合同详情：按合同上下文承载全部操作
审批中心
  待办 / 已办 / 我发起
系统管理
  合同分类配置 / 模板配置 / 外部连接 / 权限与审计
```

- 工作台不复制台账；台账不复制详情；详情不拆出客户、供应商、AI 或附件独立菜单。
- 空页签不显示；按钮由状态、权限和字段完整性共同决定，后端校验是最终依据。
- 页面采用稳定摘要 DTO；类型扩展字段通过 `form-schema` 动态渲染，新增类型字段不改通用页面代码。

### 11.6 外部适配与 Mock

- `TradePartyProvider`：搜索、详情、可用性校验；覆盖员工、客户、供应商、合作方来源。
- `OurSubjectProvider`：查询可签约我方主体。
- `FileProvider`：上传票据、确认、附件关联、预览链接、下载链接、归档。
- `ApprovalProvider`：提交、查询、撤回、转交、回调同步。
- `AiProvider`：解析、审查、查询任务、读取结果、取消任务、连接测试。
- Mock 必须覆盖成功、超时、重复请求、乱序回调、不可用、错误码、低置信度和 Schema 错误；正式接口只替换 Adapter 实现。

### 11.7 AI 专家体一期边界

- 只提供合同解析和法律风险审查，不审批、不签署、不修改合同状态、不自动确认业务字段。
- Provider 统一返回任务 ID、状态、Schema 版本、原文版本和结果版本；系统校验 Schema 后再入库。
- 解析结果和审查结果必须绑定合同、正文版本、合同类型、任务类型、审查视角和 Provider 版本。
- 低置信度、无原文依据、超时和 Schema 错误进入待人工确认或失败，不默认生成低风险。
- 先实现 Mock 和评测集，再接 Dify；Dify 私有化仅作为 Provider 实现，不进入领域层。

### 11.8 权限、安全与测试底线

- 后端每次查询和写操作重新校验菜单、动作、组织范围、合同类型、字段、合同级授权、状态和版本。
- 合同原文、身份证号、薪资、完整 AI 输入和密钥不得写入普通日志；密钥密文保存。
- 文件访问先校验合同权限，再申请短期链接；生产、测试和本地环境隔离数据、文件空间和凭证。
- 测试必须覆盖：创建/编辑/重复提交、权限越权、版本冲突、作废/撤回、Mock 故障、AI 重试与人工确认、审批乱序回调、敏感字段脱敏和审计完整性。

### 11.9 开发完成定义

第一切片完成条件：合同类型读取、交易对方选择、创建草稿、台账查询、详情查看、再次编辑和操作审计全部可运行，接口测试通过，前端页面可完成主流程，Mock 可重复运行。

一期完成条件：正文/附件、AI 解析/审查、审批适配、签署生效、履行收付款、变更补充、作废终止、权限审计、商用安全和外部联调验收全部通过。本章完成后不再新增设计文件，代码以本文件和 DOCX 为准。
