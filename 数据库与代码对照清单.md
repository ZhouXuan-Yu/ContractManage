# 数据库设计与当前代码对照清单

> 依据：`D:\project\hr-contract\数据库表\合同管理.docx`。本清单用于约束后续开发，DOCX 是权威设计，当前代码仅为早期 Mock 实现。

## 一、核心结论

你的数据库设计已经覆盖合同全生命周期，不需要重新设计。当前需要改造的是后端实现和接口，使其真正使用 DOCX 中的表、字段和状态模型。

| 业务能力 | DOCX 权威设计 | 当前代码 | 结论 |
| --- | --- | --- | --- |
| 合同主数据 | `t_contract_main` | `contract_draft` | 需要迁移实现 |
| 合同业务状态 | `contract_status` 数值状态 | `status` 文本状态 | 需要建立状态映射并逐步替换 |
| 审批状态 | `approval_status` 独立字段 | `approval_mock` 独立表，合同主表无审批字段 | 需要补齐主表关联与轮次 |
| 正文版本 | `t_contract_content_version`，含提交审批/签署标记 | `contract_content_version`，只有基础版本字段 | 需要补齐版本来源和业务标记 |
| 附件 | `biz_attach_type`、`signed_file_id`、文件对象 ID、权限规则 | `contract_attachment` 只有文件名和本地路径 | 需要按设计扩展 |
| AI 解析 | 独立任务状态、版本、哈希、确认信息 | `ai_task` 简化文本状态 | 需要建立数值状态映射 |
| AI 审查 | 独立审查任务、风险明细、法务确认 | `ai_review` 简化 JSON | 需要保留原始结果和确认版本 |
| 履行事项 | 负责人、事项类型、计划/实际日期、佐证附件 | 简化为名称、日期、状态 | 需要补齐字段 |
| 收付款计划 | 期次、条件、金额、已收付汇总、逾期计算 | 简化为名称、方向、金额、日期 | 需要补齐字段 |
| 付款记录 | 独立 `payment_record`，支持冲正和凭证 | 当前没有付款记录表 | 后续新增实现 |
| 变更/补充协议 | 独立变更关系和审批生效链路 | 当前没有实现 | 后续新增实现 |

## 二、DOCX 中已经具备的关键字段

合同主表已有：

- `contract_status`
- `approval_status`
- `sign_date`
- `effective_date`
- `origin_file_id`
- `signed_file_id`
- `current_content_version_id`
- `ai_extract_status`
- `ai_review_status`
- `current_extract_task_id`
- `current_review_id`
- `void_time`、`void_by`、`void_reason`

附件表已有：

- `attach_type`
- `biz_attach_type`，包含原件、签署文件、证明材料、变更协议附件
- `attach_version`
- `content_version_id`
- `permission_tag`
- `permission_rule`
- `storage_object_id`
- `file_hash`
- `is_main_file`

因此，前面提出的“附件没有类型”只适用于当前简化代码，不适用于你的权威数据库设计，后续以 DOCX 为准。

## 三、状态流程依据

合同业务状态：

```text
草稿 → 待签署 → 履行中 → 已完成
草稿 / 待签署 → 作废
履行中 → 已终止
待签署撤回 → 草稿
```

审批状态、AI 解析状态、AI 审查状态和履行状态均独立保存，不能把“审批中”直接当成合同业务状态。

待签署进入履行中必须满足：

1. 已有审批通过结果
2. 已上传并确认签署文件
3. 已记录签署日期和生效日期
4. 绑定签署时使用的正文版本
5. 写入状态变更和操作审计

## 四、后续改造顺序

1. 先建立数据库字段和代码模型映射，保留现有 Mock 数据兼容层。
2. 改造合同主表查询和详情 DTO，接入业务状态、审批状态、签署/生效字段。
3. 改造附件上传，支持业务附件类型和签署文件绑定。
4. 实现审批通过、签署文件确认、生效和撤回流程。
5. 再改造正文版本、AI、履行收付款和变更模块。
6. 最后按新 DTO 调整详情页，不再让前端自行推断业务状态。

## 五、明确不做的事情

- 不重新设计你的数据库字段。
- 不把 AI、审批、附件拆成独立一级菜单。
- 不用前端隐藏按钮代替后端状态和权限校验。
- 不在没有对应字段和接口时伪造“签署完成”或“确认生效”。
