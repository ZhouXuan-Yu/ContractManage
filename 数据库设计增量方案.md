# 通用合同管理系统数据库设计增量方案

> 状态：历史增量设计参考，不是当前数据库基线
>
> 依据：`合同管理系统需求设计.md`、`合同管理系统需求思维导图.md` 以及《人力资源管理系统数据库设计文档4》。
>
> 说明：本文记录某一历史版本的数据库增量设想，仅供追溯设计过程。当前表数量、字段、索引和约束统一以用户持续维护的 `D:\project\hr-contract\数据库表\合同管理.docx` 当前版本为准，本文不得直接合并或覆盖 DOCX。

## 一、设计结论

本文历史版本曾计划新增业务表并调整已有表字段；该数量和清单不代表当前设计。

```text
原有业务表
    + 历史版本规划的新增表
    = 历史版本的设计口径
```

当前实际总数不在本文维护，必须直接读取 `合同管理.docx` 当前版本。

所有新增表遵循统一规范：

- 主键使用 `BIGINT` 雪花 ID，不使用自增。
- 表名和字段名使用 `snake_case`。
- 不建立物理外键，通过业务 ID 关联。
- 业务表保留 `deleted` 逻辑删除字段。
- 保留 `create_by / create_time / update_by / update_time` 审计字段。
- 角色、用户、组织、字典等基础数据优先复用 HR 主系统，不复制基础 RBAC。

## 二、新增业务表

### 2.1 `t_contract_type_permission`

角色与合同类型权限表。合同类型是实际授权单位，合同大类不单独作为运行时权限条件；大类批量授权时展开保存为当前类型权限。

核心字段：

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | BIGINT | 主键 |
| `role_id` | BIGINT | HR 主系统角色 ID |
| `contract_type_id` | BIGINT | 合同类型配置 ID |
| `contract_subtype_id` | BIGINT | 合同子类型 ID，可为空表示该类型下全部子类型 |
| `permission_action` | VARCHAR(32) | 权限动作：`create` / `view` / `handle` |
| `enabled` | TINYINT | 是否启用 |
| `version` | INT | 配置版本 |
| `remark` | VARCHAR(500) | 备注 |

建议索引：`uk_role_type_subtype_action`、`idx_role_id`、`idx_contract_type_id`。

规则：新增合同类型不自动继承已有角色权限；停用权限不影响历史合同，只影响新建和后续操作校验。

### 2.2 `t_contract_template_permission`

角色与合同模板使用权限表。

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | BIGINT | 主键 |
| `role_id` | BIGINT | HR 主系统角色 ID |
| `template_id` | BIGINT | 合同模板 ID，可为空表示按模板分类或场景授权 |
| `template_category_dict_code` | VARCHAR(64) | 模板分类字典编码，可为空 |
| `permission_action` | VARCHAR(32) | `use` / `view` / `manage` |
| `enabled` | TINYINT | 是否启用 |
| `version` | INT | 配置版本 |

规则：无模板使用权限时，模板不出现在新建合同和常用模板入口；模板权限不替代合同类型权限。

### 2.3 `t_contract_field_permission`

角色与字段权限表，用于敏感字段和类型扩展字段的展示、编辑控制。

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | BIGINT | 主键 |
| `role_id` | BIGINT | HR 主系统角色 ID |
| `field_code` | VARCHAR(64) | 统一字段字典编码 |
| `visible_flag` | TINYINT | 是否可见 |
| `read_only_flag` | TINYINT | 是否只读 |
| `mask_flag` | TINYINT | 是否脱敏 |
| `editable_flag` | TINYINT | 是否可编辑 |
| `enabled` | TINYINT | 是否启用 |
| `version` | INT | 配置版本 |

建议唯一键：`uk_role_field_code`。字段权限不能突破合同本身的数据权限；有合同查看权不代表可以查看全部字段。

### 2.4 `t_contract_object_auth`

合同级临时授权表，用于跨组织协作和指定合同的例外授权。

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | BIGINT | 主键 |
| `contract_id` | BIGINT | 合同 ID |
| `subject_type` | TINYINT | 授权对象：用户 / 角色 |
| `subject_id` | BIGINT | 用户 ID 或角色 ID |
| `permission_action` | VARCHAR(32) | `view` / `edit` / `ai_review` / `export` |
| `start_time` | DATETIME | 生效时间 |
| `end_time` | DATETIME | 失效时间 |
| `grant_reason` | VARCHAR(500) | 授权原因，必填 |
| `status` | TINYINT | 有效 / 撤销 / 过期 |
| `revoked_by` | BIGINT | 撤销人 |
| `revoked_time` | DATETIME | 撤销时间 |

建议索引：`idx_contract_id`、`idx_subject`、`idx_valid_time`。

规则：合同级授权只扩大指定合同的受控操作范围，不能扩大系统管理权限；授权、撤销和过期均写入审计日志。

### 2.5 `t_contract_perform_item`

合同通用履行事项表。它与收付款计划不同：履行事项记录里程碑、交付、验收、服务节点等业务动作，付款计划只记录应收应付安排。

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | BIGINT | 主键 |
| `contract_id` | BIGINT | 合同 ID |
| `item_type` | TINYINT | 事项类型：里程碑 / 交付 / 验收 / 服务节点 / 其他 |
| `item_name` | VARCHAR(255) | 事项名称 |
| `item_description` | VARCHAR(1000) | 事项说明 |
| `planned_date` | DATE | 计划日期 |
| `actual_date` | DATE | 实际完成日期 |
| `responsible_user_id` | BIGINT | 负责人 |
| `status` | TINYINT | 未开始 / 进行中 / 已完成 / 已逾期 / 已取消 |
| `evidence_attachment_id` | BIGINT | 佐证附件 ID，可为空 |
| `sort_no` | INT | 展示顺序 |

建议索引：`idx_contract_id`、`idx_status_planned_date`。

规则：事项可按合同类型配置生成；非金额型合同可以只有履行事项而没有付款计划。

### 2.6 `t_contract_our_subject`

我方主体配置表。只保存可选我方法人的外部主数据引用和必要展示信息，不替代法人主数据系统。

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | BIGINT | 主键 |
| `subject_source_type` | VARCHAR(32) | 来源系统 |
| `subject_external_id` | VARCHAR(128) | 外部我方主体 ID |
| `subject_name_snapshot` | VARCHAR(255) | 当前展示名称快照 |
| `subject_nature` | TINYINT | 企业 / 其他组织 |
| `enabled` | TINYINT | 是否可用于新合同 |
| `is_default` | TINYINT | 是否默认我方主体 |
| `sort_no` | INT | 展示顺序 |
| `remark` | VARCHAR(500) | 备注 |

建议唯一键：`uk_source_external_id`；索引：`idx_enabled`。

规则：一期可以按单一我方主体使用；数据模型预留多法人。停用后不能用于新合同，历史合同通过 `t_contract_main` 的引用继续保留。

## 三、已有表增加字段

| 表 | 新增字段 | 类型 | 说明 |
|---|---|---|---|
| `t_contract_main` | `our_subject_source_type` | VARCHAR(32) | 我方主体来源系统 |
| `t_contract_main` | `our_subject_external_id` | VARCHAR(128) | 外部我方主体 ID；与 `t_contract_our_subject` 对应 |
| `t_contract_approval_record` | `approval_provider` | VARCHAR(32) | 审批提供者：`mock` / 正式审批系统 |
| `t_contract_ai_extract_task` | `task_provider` | VARCHAR(32) | AI 任务提供者，当前为 Dify 或 Mock |
| `t_contract_ai_review` | `task_provider` | VARCHAR(32) | AI 审查任务提供者，当前为 Dify 或 Mock |
| `t_contract_export_log` | `export_scope_type` | VARCHAR(32) | 导出范围：当前筛选 / 当前合同 / 批量等 |
| `t_contract_export_log` | `auth_check_result` | TINYINT | 导出权限校验结果：通过 / 拒绝 |
| `t_contract_template` | `template_category_dict_code` | VARCHAR(64) | 模板自身分组分类字典编码，不等于合同大类或合同类型 |

建议为 `t_contract_main` 增加索引 `idx_our_subject`；为模板分类增加 `idx_template_category`；导出日志增加 `idx_auth_result` 和 `idx_export_scope`。

## 四、与权限公式的关系

一期默认可见合同：

```text
HR基础组织数据范围
  ∩ t_contract_type_permission 中的合同类型权限
  ∩ deleted=0
```

这里的 `deleted=0` 是数据保留条件，不等于合同未作废。作废合同仍按权限展示和审计，但不计入有效业务统计。合同级授权通过 `t_contract_object_auth` 作为指定合同的受控例外，不改变基础权限公式。

## 五、历史同步说明

历史上曾计划将本增量方案合并进 `D:\project\hr-contract\数据库表\合同管理.docx`。当前不执行该合并，若需参考必须逐项与 DOCX 当前版本核对：

1. 表清单和总表数。
2. 6 张新增表的字段、唯一键、普通索引和逻辑删除规则。
3. 6 张已有表的新增字段和索引。
4. 表与合同大类、合同类型、角色、模板、字段字典、履行事项的关系说明。
5. 版本号和变更记录。
