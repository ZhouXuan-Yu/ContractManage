# 公司平台接口接入与报文说明

> 更新时间：2026-09-09
>
> 本文根据 `D:\project\hr-contract\数据库表` 下的接口文档整理，仅记录已从文档确认的内容。没有平台地址、应用编码、Token 或正式接口说明的部分标记为“待平台确认”，不得用测试值宣称正式接入完成。

## 1. 当前结论

当前项目暂时不能直接调用公司平台接口，原因如下：

- 已取得 DEV 网关地址：MDM 为 `https://mdm-iam-dev.qd-ecs.com:28084`，文档中心为 `https://doc-iam-dev.qd-ecs.com:28084`。
- 文档中描述的直连地址 `18086`、`18087` 仍是服务内网直连占位符，当前不作为本机调用地址。
- 网关 OpenAPI 地址仍未确认；将业务域名拼接文档中的 `/v3/api-docs/...` 返回 `404`。
- 受保护接口需要 IAM `accessToken`。
- 下游调用需要公司分配的 `X-App-Code`。
- 本机检查 `127.0.0.1:18084`、`18086`、`18087` 均未开放。
- 实测 `https://mdm-iam-dev.qd-ecs.com:28084/api/v1/mdm/categories/list?status=1` 返回 `401`。
- 实测 `https://doc-iam-dev.qd-ecs.com:28084/api/v1/docs/tree/libraries/by-app?appCode=contract-system` 返回 `401`。
- 两个服务的 `/health` 均返回 `404`，不能据此判断服务不可达；业务路由已返回鉴权响应，说明网关可达且路由存在。

因此当前可以完成接口契约整理和 Mock 联调，但不能直接读取公司真实数据，也不能直接上传真实文件。

## 2. 已确认的通用调用规则

### 2.1 请求头

受保护接口由合同系统后端调用，浏览器不直接持有平台 Token：

```http
Authorization: Bearer <access_token>
X-App-Code: <合同系统应用编码>
X-Request-Id: <本次请求唯一ID>
Content-Type: application/json
```

`X-App-Code` 的具体值必须由平台分配，不能自行填写。Token、密码和文件内容不能写入普通日志。

### 2.2 统一响应

JSON 接口成功响应结构：

```json
{
  "code": "0",
  "message": "success",
  "success": true,
  "data": {},
  "requestId": "request_xxx",
  "timestamp": "2026-09-09T10:00:00Z"
}
```

失败时以 `success=false`、`code` 和 `message` 为准。合同系统应保留 `requestId`，用于跨系统排查；不能把所有下游错误都转换成“系统异常”。

## 3. IAM 登录接口

### 3.1 BFF 登录

合同系统前端只调用合同系统自己的登录接口，合同系统后端再调用 IAM：

```http
POST /api/v1/auth/login/password
Content-Type: application/json
```

请求体：

```json
{
  "loginName": "<用户名>",
  "password": "<密码>",
  "deviceInfo": "Chrome@Windows"
}
```

响应核心字段：

```json
{
  "code": "0",
  "success": true,
  "data": {
    "sessionId": "sess_xxx",
    "accessToken": "at_xxx",
    "refreshToken": "rt_xxx",
    "expiresIn": 7200,
    "userId": 1912345678901234567,
    "roleIds": [1912345678901400001],
    "isSuperAdmin": false,
    "passwordResetRequired": false
  }
}
```

合同系统后端应使用 HttpOnly 会话保存登录状态，不能把 Token 存到浏览器 `localStorage`。

### 3.2 会话和权限上下文

```http
GET /api/v1/auth/session/current
Authorization: Bearer <access_token>
```

```http
GET /api/v1/auth/context/my
Authorization: Bearer <access_token>
```

当前仍需平台确认：Token 有效期、刷新接口、用户组织字段、岗位角色字段、退出接口、issuer、audience 和 JWKS 地址。

## 4. MDM 元数据接口

服务：`iam-mdm-service`

- DEV 网关基地址：`https://mdm-iam-dev.qd-ecs.com:28084`
- 网关路径：`https://mdm-iam-dev.qd-ecs.com:28084/api/v1/mdm/...`
- OpenAPI：待确认实际网关管理地址，当前拼接到业务域名返回 `404`

已确认的查询接口：

```http
GET /api/v1/mdm/enums/list?status=1
GET /api/v1/mdm/categories/list?status=1
GET /api/v1/mdm/metadata/?pageNo=1&pageSize=20&status=1
GET /api/v1/mdm/metadata/{id}
GET /api/v1/mdm/metadata/{id}/versions
```

重要边界：当前文档描述的是元数据分类、枚举和字段定义，不是客户、供应商或员工主数据接口。合同交易对方不能直接使用 MDM 元数据接口代替主体业务查询接口。

待平台补充真实的主体接口：分页搜索、稳定 ID 查询、主体状态、版本、字段权限、客户/供应商/员工类型及数据范围。

## 5. 文档中心接口

服务：`iam-doc-service`

- DEV 网关基地址：`https://doc-iam-dev.qd-ecs.com:28084`
- 网关路径：`https://doc-iam-dev.qd-ecs.com:28084/api/v1/docs/...`
- OpenAPI：待确认实际网关管理地址，当前拼接到业务域名返回 `404`

### 5.1 查询合同系统文档库

```http
GET /api/v1/docs/tree/libraries/by-app?appCode=<合同系统应用编码>
Authorization: Bearer <access_token>
X-App-Code: <合同系统应用编码>
X-Request-Id: <request_id>
```

应用编码没有对应文档库和 ACL 时，应直接报配置错误，不能自动使用其他应用的文档库。

### 5.2 文件登记

文档目前明确的是“登记已经存在的文件”，请求体核心字段为：

```http
POST /api/v1/docs/libraries/{libraryId}/files
Content-Type: application/json
```

```json
{
  "parentItemId": 0,
  "fileId": 123456,
  "itemName": "合同审批版.pdf",
  "securityLevelCode": "<平台分级编码>",
  "syncKnowledgeFlag": 0,
  "expertId": null
}
```

这里的 `fileId` 必须先由平台原始文件上传接口产生。现有文档没有提供完整的原始上传接口，因此当前不能宣称“文件上传到公司文件中心”已经打通。

### 5.3 文件查询、预览和下载

```http
GET /api/v1/docs/libraries/{libraryId}/items/{itemId}
GET /api/v1/docs/libraries/{libraryId}/items/{itemId}/preview
GET /api/v1/docs/libraries/{libraryId}/items/{itemId}/download
```

更新文件关联：

```http
PUT /api/v1/docs/libraries/{libraryId}/items/{itemId}/file
Content-Type: application/json
```

```json
{
  "fileId": 123457
}
```

合同系统仍需在自己的数据库中保存合同 ID、正文版本、业务用途（原始文件/审批版/签署版/证明材料）、文档库 ID、文档项 ID、平台文件 ID、文件指纹和版本关系。

## 6. 合同系统的正确接入顺序

```text
门户或合同系统登录
  -> IAM 获取 accessToken
  -> 合同系统后端建立 HttpOnly 会话
  -> 查询当前用户和组织权限
  -> 查询应用对应文档库
  -> 原始文件上传服务取得 fileId
  -> 文档中心登记 fileId 取得 itemId
  -> 合同系统保存文件关系和正文版本
  -> 合同审批/履约继续使用合同系统业务接口
```

浏览器不应直接调用 IAM、文档中心或其他平台服务，也不应携带 `X-App-Code` 和平台 Token。

## 7. 当前可做与不可做

### 可以先做

- 根据上述报文建立后端适配器和 Mock 契约测试。
- 用平台提供的真实地址测试健康接口和 OpenAPI 文档地址。
- 用真实 Token 测试 MDM 元数据查询。
- 用真实应用编码查询合同系统对应文档库。
- 对文档登记、预览、下载和权限错误做接口联调。

### 目前不能直接做

- 不能使用占位地址直接获取真实数据。
- 不能没有 `accessToken` 和 `X-App-Code` 调用受保护接口。
- 不能把 MDM 元数据接口当成供应商/员工主数据接口。
- 不能在没有原始上传接口的情况下生成真实 `fileId`。
- 不能把文档登记接口成功等同于文件上传闭环完成。

## 8. 需要平台提供的联调参数

1. 网关域名和各环境地址。
2. IAM 登录或 OIDC 参数、测试账号和 Token 获取方式。
3. ContractManage 的 `X-App-Code`。
4. IAM 用户、组织、岗位和角色字段映射。
5. 供应商/客户/员工真实主体查询接口。
6. 原始文件上传接口、multipart 字段、大小和类型限制、hash、病毒扫描及失败清理规则。
7. ContractManage 文档库和 ACL 初始化结果。
8. 审批发起、查询、撤回、回调地址、签名和事件幂等规则。

## 9. 来源文件

- `D:\project\hr-contract\数据库表\iam-mdm-service-API对接文档.md`
- `D:\project\hr-contract\数据库表\iam-doc-service-API对接文档.md`
- `D:\project\hr-contract\数据库表\第三方系统标准接入说明-自有登录页-BFF模式-v1.0.md`
- `D:\project\hr-contract\ContractManage-main\docs\公司门户与平台服务对接开发指南.md`
