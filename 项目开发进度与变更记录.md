# 项目开发进度与变更记录

## 54. 文件中心可替换上传适配链（2026-09-08）
- 新增 `DocumentCenterBinaryUploadClient`：定义二进制上传输入和返回 `fileId` 的唯一边界；当前 `UnavailableDocumentCenterBinaryUploadClient` 会明确拒绝未配置的真实上传，不会伪造成功结果。
- 新增 `DocumentCenterFileStorageProvider`：启用 `CONTRACT_FILE_PROVIDER=document` 后，先通过上传 Client 获取 `fileId`，再调用文件中心已确认的 `POST /api/v1/docs/libraries/{libraryId}/files` 登记合同文档，保存 `doc:{libraryId}:{itemId}` 引用。
- 默认 `CONTRACT_FILE_PROVIDER=local`，继续使用本地 Provider；已在 `backend/.env.iam.example` 写入文件中心网关、文档库、应用编码和服务令牌变量，均为空且不含真实凭证。
- 平台补齐二进制上传接口后，仅需新增该接口对应的 `DocumentCenterBinaryUploadClient` 实现，并设置 `CONTRACT_FILE_PROVIDER=document`，合同附件、审批、履约、变更均无需修改。
- 验证：后端 Maven 测试通过（7 个测试）。未启动、重启或停止应用服务。

## 53. 统一平台集成收口：文件中心适配准备（2026-09-08）
- 复核 IAM 与文件中心资料：IAM 密码登录、OIDC 回调和受限 BFF 已存在；文件中心已提供文档库登记、预览和下载接口，但其登记接口要求调用方先取得 `fileId`，权威文档未提供二进制上传并返回该 ID 的接口。
- 附件保存改为统一 `FileStorageProvider`：合同服务只接收 Provider 返回的文件引用、存储路径、文件名和大小，不再在业务服务内直接决定文件目录；默认 `LocalFileStorageProvider` 保证当前 SQLite 开发流程可继续运行。
- 新增文件中心配置项 `DOCUMENT_CENTER_GATEWAY_BASE_URL`、`DOCUMENT_CENTER_LIBRARY_ID`、`DOCUMENT_CENTER_APP_CODE`。集成状态会准确显示本地降级或文件中心配置就绪，不会把未完成的真实上传标记为可用。
- 真实文件中心切换前置条件：平台需补充“二进制上传或预签名上传，返回 fileId”的接口契约、测试文档库 ID 和网关授权方式；取得后只需新增 Document Provider，不改合同、审批、履约和附件关系。
- 验证：后端 Maven 测试通过（7 个测试）；前端构建通过。未启动、重启或停止应用服务。

## 52. 本地开发端口兼容（2026-09-08）
- 发现 PID `22108` 持续占用多个前端端口，取消固定端口与严格端口限制，恢复 Vite 自动选择空闲端口。
- 后端 CORS 改为仅允许本机 `localhost` 与 `127.0.0.1` 的任意开发端口，确保 Vite 自动换端口后本地登录请求仍可携带会话凭证。

## 51. 本地端口最终调整（2026-09-08）
- `5180` 也被 PID `22108` 占用，前端开发端口改为已检查空闲的 `6201`；后端 CORS 同步允许 `localhost/127.0.0.1:6201`。

## 50. 本地端口冲突修正（2026-09-08）
- 发现 `5178` 已被 PID `22108` 占用，未停止该进程；前端开发端口改为检查后空闲的 `5180`，后端 CORS 同步调整为该来源。

## 49. 本地前端端口调整（2026-09-08）
- 前端开发服务固定为 `5178`，并启用严格端口，不再因 `5173` 被其他项目占用而自动切换端口。
- 后端 CORS 白名单新增 `localhost:5178` 与 `127.0.0.1:5178`，保留现有 `5173` 来源兼容。

## 48. 合同变更、补充协议、归档与分析（2026-09-08）
- 变更正文改为候选版本：创建变更时只新增非当前正文版本，原合同有效正文仅在审批通过并确认生效后切换。
- 变更审批支持提交、模拟通过、模拟驳回和撤回；审批动作带变更 ID 和候选正文版本独立写入记录，不影响主合同审批。
- 新增“已完成”状态；仅已完成或已终止合同可以归档。归档为非删除标记，历史合同、凭证、审批和审计继续可查。
- 归档后，正文、附件、履约、付款和变更的写入均由后端拒绝；标记仅保留在兼容层，不修改 DOCX 权威主表结构。
- 分析在既有权限范围内汇总合同金额、变更数量、归档数量与已确认实际收付款金额，经营指标不包含已归档合同。
- 验证：后端 Maven 测试通过（7 个测试）；前端构建通过。未启动、重启或停止应用服务。

### 本轮未纳入
异步证据包、Legal Hold、自动归档、真实审批回调、公司文档中心正式文件关联，等待相关平台接口到位后接入。

> 用途：记录实际实现的导航模块、代码改动、验证结果、已确认的产品决策和后续缺口。
>
> 权威依据：`合同管理系统需求设计.md`、`合同管理系统需求思维导图.md`、`D:\project\hr-contract\数据库表\合同管理.docx`、`开发实施规划.md`。
>
> 更新原则：每完成一个左侧导航模块，在本文件追加记录；数据库、流程、接口契约或权限规则需要调整时，先与产品负责人确认。

## 1. 已确认的协作与设计原则

- 目标是可商用的企业合同管理系统，不做展示型页面。
- 一个完整开发模块以左侧导航栏为单位，不以按钮、字段或详情页操作为单位拆分。
- 页面应简洁、正式、充分使用主工作区；不得将主要内容局限在页面中央的小卡片中。
- 合同正文、附件、AI、审批、签署、履行和变更是合同详情内的业务功能，不单独作为一级导航模块。
- 数据库 DOCX 是字段和表结构的权威来源。当前 Mock 表仅为过渡兼容层，不能据此否定 DOCX 设计。
- 每轮开始前说明模块目标、页面范围、依据、字段/接口/权限、数据库影响和验收标准；完成后主动说明下一模块。

## 2. 当前导航模块状态

| 导航分组 | 模块 | 状态 | 当前能力 |
| --- | --- | --- | --- |
| 合同中心 | 工作台 | 已完成初版 | 指标、待处理入口、最近更新合同和快捷入口 |
| 合同中心 | 合同台账 | 已完成初版 | 分类/类型切换、筛选、查询、选择、CSV 导出、新建和详情入口 |
| 合同中心 | 履行与收付款 | 已完成初版 | 履行节点、收付款计划、完成登记、逾期提示、审计记录 |
| 合同统计 | 统计分析 | 待完善 | 当前只有基础状态指标，金额、风险、履行统计待接正式 DTO |
| 系统管理 | 合同分类配置 | 已完成初版 | 三级目录、启停、版本发布、下级分类和规则摘要 |
| 系统管理 | 合同模板配置 | 已完成初版 | 模板查询、编辑、变量识别、版本、发布和启停 |
| 系统管理 | 外部连接配置 | 已完成初版 | 连接目录、编辑、启停、测试、脱敏密钥和连接日志 |
| 系统管理 | 权限与审计 | 下一模块 | 角色、数据范围、敏感字段、全局审计待实现 |

## 3. 已实施功能与改动

### 3.1 登录与会话

- 前端实现本地 Mock 登录、登录态持久化和退出登录。
- 未登录时不加载业务数据。
- 当前演示账号：`admin` / `admin123`。
- 不提供公开注册；正式环境应对接 HR/SSO。

主要改动：`frontend/src/App.vue`、`frontend/src/styles.css`。

### 3.2 合同中心

- 合同台账工作区：`frontend/src/components/ContractLedgerWorkspace.vue`。
- 履行与收付款工作区：`frontend/src/components/FulfillmentWorkspace.vue`。
- 合同详情已具备正文版本、附件、AI 解析/审查、审批、履行、收付款、变更与补充协议入口。
- 签署生效链路已补充签署文件类型、签署日期、生效日期；审批通过、存在签署文件且填写日期后才可进入履行中。

后端相关改动：`ContractService.java`、`FulfillmentService.java`、`ChangeService.java`、`AiWorkflowService.java`、`ContractController.java`、`ContractModels.java`。

### 3.3 合同分类配置

- 新增 `CategoryWorkspace.vue` 与 `CategoryService.java`。
- 提供三级分类目录、大类/类型/子类型维护、启用停用和版本发布。
- 接口：`GET/POST /api/categories`、`PUT /api/categories/{id}`、`POST /api/categories/{id}/toggle`、`POST /api/categories/{id}/publish`。
- 管理权限：`TEMPLATE`。

### 3.4 合同模板配置

- 新增 `TemplateWorkspace.vue`，并扩展 `TemplateService.java` 与 `ContractController.java`。
- 支持列表、关键词/类型/状态筛选、新建、正文编辑、变量识别、版本历史、发布与启停。
- 已与新建合同页面的已发布模板选择联动。
- 接口补充：`POST /api/templates/{id}/toggle`、`GET /api/templates/{id}/versions`。

### 3.5 外部连接配置

- 新增 `ConnectionWorkspace.vue`，替代原“外部连接”骨架页。
- 新增 `ConnectionService.java`、`t_external_connection`、`t_external_connection_log`。
- 默认提供 HR/组织、交易对方主数据、我方主体、文件服务、外部审批、AI Provider 六类适配器记录。
- 支持编辑、新建、启用/停用、测试连接、最近测试时间、失败摘要和连接日志。
- 密钥仅保存服务端配置状态，接口只返回“是否已配置”，不返回明文，也不写入普通日志。
- 接口：
  - `GET/POST /api/connections`
  - `PUT /api/connections/{id}`
  - `POST /api/connections/{id}/test`
  - `POST /api/connections/{id}/toggle`
  - `GET /api/connections/{id}/logs`
- 权限：`TEMPLATE`。

主要改动：

- `frontend/src/components/ConnectionWorkspace.vue`
- `frontend/src/App.vue`
- `frontend/src/styles.css`
- `backend/src/main/java/com/hrcontract/application/ConnectionService.java`
- `backend/src/main/java/com/hrcontract/application/ContractModels.java`
- `backend/src/main/java/com/hrcontract/api/ContractController.java`

### 3.6 正式表兼容层

- 新增 `FormalSchemaMigration.java`，保留现有 Mock 表并建立正式表适配层，避免破坏已有演示数据。
- 已创建并迁移部分正式表：`t_contract_main`、`t_contract_content_version`、`t_contract_attachment`、`t_contract_change`、`sys_approval_instance`、`t_contract_approval_record`。
- 详细映射和缺口见 `数据库与代码对照清单.md`。

## 4. 已验证结果

截至外部连接模块完成：

- 前端：`npm.cmd run build` 通过。
- 后端：`mvn.cmd "-Dmaven.repo.local=D:\project\hr-contract\Hrcontract\.m2-local" -DskipTests compile` 通过。
- Maven 初始环境强制写入 `C:\.m2\repository` 会失败；指定项目本地仓库后可正常编译。

## 5. 当前已知缺口与风险

- 正式 DOCX 表已完成部分初始化和数据复制，但多数页面和接口仍读取旧 Mock 表，需按模块逐步切换，不可直接删除旧表。
- 外部连接的测试当前验证适配器配置完整性，不会真实请求第三方服务；真实联调需要接口地址、认证方式和对方接口文档。
- 统计分析尚未接入金额、风险、到期、履行等正式口径。
- 模板已发布后的启停按钮需在下一次模板模块回归时确认界面行为。
- 当前登录和组织数据仍为 Mock；正式部署应接 HR/SSO 和组织架构来源。

## 6. 下一导航模块：权限与审计

已确认采用“角色权限 + 数据范围”两层控制，第一阶段使用内置模拟组织和人员数据。

第一阶段数据范围：

1. 全部数据。
2. 所属组织。
3. 所属部门。
4. 本人创建及参与合同。
5. 指定合同范围。
6. 敏感字段访问控制。

拟实现范围：角色配置、菜单与动作权限、数据范围配置、敏感字段权限、全局审计查询、按用户/模块/动作/时间筛选、审计详情与导出，并与现有 `AccessControl` 统一。

## 7. 权限与审计模块（已完成初版）

- 新增角色权限配置：`ADMIN`、`LEGAL`、`BUSINESS`、`VIEWER`。
- 支持页面与动作权限、默认数据范围、合同类型权限、敏感字段访问开关。
- 一期数据范围：全部数据、所属组织、所属部门、本人及参与合同、指定合同范围。
- `AccessControl` 优先读取可配置角色权限，保留默认权限作为初始化兼容。
- 新增全局审计查询，统一显示合同操作日志和权限配置变更日志，并支持模块和关键词筛选。
- 新增接口：
  - `GET /api/permission-roles`
  - `PUT /api/permission-roles/{code}`
  - `GET /api/audit-logs`
- 主要文件：
  - `frontend/src/components/PermissionAuditWorkspace.vue`
  - `backend/src/main/java/com/hrcontract/application/PermissionAuditService.java`
  - `backend/src/main/java/com/hrcontract/api/AccessControl.java`
  - `backend/src/main/java/com/hrcontract/api/ContractController.java`
- 验证：前端 `npm.cmd run build` 通过；后端 Maven compile 通过。

仍未完成：真实 HR/SSO 用户和组织来源、查询时真实数据范围过滤、合同级授权有效期、文件/AI/审批/导出审计事件统一接入。这些需要正式 Adapter 契约或正式表读取切换时继续实现。

## 8. 统计分析模块（2026-08-31）

- 页面文件：\`frontend/src/components/StatisticsWorkspace.vue\`；后端服务：\`StatisticsService.java\`；接口：\`GET /api/statistics/overview\`。
- 统计内容：有效合同与生命周期、履行节点、收付款计划及 AI 任务/风险概览；作废合同保留可查但不计入有效汇总。
- 前端结果可点击进入合同台账状态筛选，履行数据可进入履行与收付款模块；页面包含加载、错误和刷新状态。
- 权限：接口要求 \`VIEW\`，非 ADMIN 角色应用配置的合同类型权限；组织范围和合同级授权仍需接入真实 HR/SSO 数据后完成。
- 数据口径：合同金额/签署日期/到期日期尚未从 DOCX 正式合同主表切换，页面明确不展示伪造的合同金额；收付款金额仅来自现有付款计划。
- 验证：前端 \`npm.cmd run build\` 通过；后端 Maven compile 通过。旧运行实例在未重启新类前返回统计接口 500，需重启后端后复测。

## 9. 下一步：正式表字段切换与合同详情闭环

- 先按 DOCX 核对并映射合同金额、币种、签署/生效/到期日期及数据权限字段。
- 再将台账、详情、工作台和统计逐步切换到正式表适配层，保留旧 Mock 表兼容，不删除历史数据。

## 10. 应用壳与导航视觉调整（2026-08-31）

- 参考公开企业后台的通用信息架构，重做浅色侧边栏、分组折叠导航、当前页状态和全宽主工作区。
- 合同中心、合同统计、系统管理及其子菜单不变；不复制参考图的品牌、插画、图标、文案或像素级稿件。
- 工作台主副栏比例、面板边界、顶栏和移动端导航规则同步调整。
- 验证：frontend npm.cmd run build 通过。

## 11. 全局壳完整交付块（2026-08-31）

- 对应参考：第三张图的深色系统品牌区、独立浅色侧栏、分组导航和右侧主工作区；第二张图的企业后台留白与信息层次；第一张图的流程型系统感暂留到新建合同模块。
- 修改文件：frontend/src/styles.css。
- 已实现：深色连续顶部系统栏、左侧独立导航栏、全宽主内容、桌面/移动端响应式规则。
- 未处理：工作台内部模块重新编排、台账重排、新建合同步骤化；这些属于后续完整交付块。
- 验证：frontend/npm.cmd run build 通过。

## 12. 首页是否独立的产品决策（2026-08-31）

- 当前不新增独立“首页”导航，不把“工作台”移出“合同中心”。
- 预留独立首页作为未来系统级汇总入口的可能性；只有在合同、审批、履行、AI、组织等真实数据接入后，确认存在跨模块汇总价值才启用。
- 若未来启用，导航关系为“首页”独立一级入口，“合同中心”继续包含“工作台、合同台账、履行与收付款”。
- 当前应用壳重构只处理导航表现和空间布局，不提前制作无真实数据支撑的首页。

## 13. 导航树形表现最终口径（2026-08-31）

- 合同中心、合同统计、系统管理采用一级可展开节点，不再作为灰色分组标题。
- 一级节点统一为图标、14px 标题和折叠三角；二级节点统一为轻图标、13px 文本、缩进和当前页高亮。
- 保留当前业务导航内容和工作台归属；不新增独立首页。
- 验证：frontend npm.cmd run build 通过。

## 14. 工作台完整布局块（2026-08-31）

- 参考第二张图的主工作区与右侧辅助队列结构，对工作台内容做职责收敛。
- 保留：合同概况指标、状态处理区、待办处理区、最近更新合同。
- 移除：重复的生命周期宣传横幅和独立大块快捷操作区。
- 数据口径：仅显示现有真实合同状态；合同金额、到期、AI 指标未可靠接入时不伪造数据。
- 未处理：合同台账和履行台账的筛选/表格/批量操作布局，属于下一完整交付块。
- 验证：frontend npm.cmd run build 通过。

## 15. 合同台账与履行台账完整布局块（2026-08-31）

- 合同台账：合同大类/类型上下文、查询筛选、选择与导出操作、全宽表格和正式字段缺口提示。
- 履行台账：左侧履行中合同选择，右侧当前合同摘要、履行节点与收付款计划维护；保留逾期、完成和撤销完成操作。
- 没有改变 API、数据库字段或业务规则，仅重排页面的操作层级、边界和响应式布局。
- 验证：frontend npm.cmd run build 通过。
- 下一完整交付块：新建合同与合同详情，采用第一张图的步骤式创建引导，并收敛详情页内的处理入口。

### 10.1 侧栏与工作台纠正

- 根据运行截图继续调整：侧栏补充入口图形、收紧分组和菜单密度、强化当前页面状态，并固定底部用户区。
- 工作台将重复的生命周期横幅压缩为操作入口条，让状态总览和待办面板成为首屏主体。
- 验证：frontend npm.cmd run build 通过。

范围：合同数量与金额、生命周期分布、履行与收付款、到期事项和 AI 风险概览；所有指标复用台账的数据范围过滤，并且可跳转到对应筛选结果。

## 16. 新建合同与合同详情完整交付块（2026-08-31）

- 完成“新建合同”三步流程：创建来源、基础信息、确认并保存草稿。
- 合同详情按任务页签整理为概览/操作记录、正文/附件/智能审查/审批、履行/收付款；变更和签署仍为上下文入口。
- 未变更数据库字段、API 契约、权限或生命周期规则；创建仍只使用既有权威字段。
- 页面纠正：台账、履行和模板在其完整工作区激活时不再显示旧内嵌页面，消除重复页面、重复操作和无效留白。
- 验证：`frontend/npm.cmd run build` 通过。

## 17. 下一完整交付块

- 正式表字段切换与真实数据范围：先依据 `D:\project\hr-contract\数据库表\合同管理.docx` 核对金额、币种、签署/生效/到期日期以及组织/人员范围字段，再与您对接确认。该块会影响数据口径和接口适配，不会直接擅自修改。

## 18. 正式字段与数据口径第一阶段（2026-08-31）

- 已集中核对 DOCX 权威表，确认合同主表字段组：合同总金额、币种、收付方向、签署日期、生效日期、到期日期、来源类型、模板版本，以及发起人和发起部门范围。
- 兼容层一次性补充金额、币种、收付方向、到期日期和来源类型，并同步到 `t_contract_main`；保留旧 Mock 表和历史数据，不做破坏性切换。
- 合同详情新增“合同正式字段”整块展示，缺失值显示“未设置”，不伪造业务数据。
- 未改变权限规则；真实 HR/组织范围过滤、发起人/部门选择和正式字段编辑需要真实主数据及字段权限接入后再实施。
- 验证：后端 `mvn.cmd "-Dmaven.repo.local=D:\project\hr-contract\Hrcontract\.m2-local" -DskipTests compile` 通过；前端 `npm.cmd run build` 通过。

## 19. 下一完整交付块

- 数据范围与正式字段编辑闭环：对接 `sys_user`、`sys_role`、`sys_role_org` 和合同参与主体，完成全部/部门/本人/自定义范围的真实查询过滤，并将正式字段纳入新建、编辑、详情、台账和统计口径。该块涉及权限和数据源边界，开始前统一对接确认。

## 20. 合同数据全流程闭环（2026-08-31）

- 正式字段贯通新建和草稿编辑：合同总金额、币种、收付方向、到期日期；后端统一校验金额、币种、方向和日期格式。
- 台账增加金额/币种和到期日期展示；未录入值保持“未设置”，不生成假业务数据。
- 数据范围统一进入台账、合同详情、履行计划和统计查询，范围与合同类型权限取交集。
- 新增本地模拟身份上下文：默认用户 `10001`、组织 `100`、部门 `101`，支持 `X-User-Id`、`X-Org-Id`、`X-Department-Id` 替换，后续可由 HR/SSO 适配器接管。
- 修复统计权限拼接缺陷：追加类型条件，不覆盖组织/本人范围条件。
- 验证：后端 Maven compile 通过；前端 npm build 通过。

## 21. 下一完整交付块

- 模拟身份与权限验收工作区：提供用户/组织/部门切换、范围命中说明、合同级授权和接口验收结果，并覆盖台账、详情、履行、统计的越权测试。完成后再接入真实 HR/SSO 适配器。

## 22. 模拟身份与权限验收工作区（2026-08-31）

- 新增“数据范围验收”完整页面，支持管理员、业务人员、法务人员三种模拟身份切换，展示用户、组织、部门和当前角色。
- 新增 `GET /api/directory/mock`，提供模拟用户、组织和部门目录；后续 HR/SSO 只替换目录适配器。
- 前端请求统一携带 `X-User-Id`、`X-Org-Id`、`X-Department-Id` 和 `X-Role`；履行、统计不再只带角色。
- 维持范围与合同类型权限取交集，并在列表、详情、履行、统计读边界执行。
- 验证：后端 Maven compile 通过；前端 npm build 通过。

## 23. 下一完整交付块

- 合同级临时授权与越权验收：一次完成指定合同授权、查看/编辑/导出/法务确认权限、有效期和撤销状态，并在详情、附件、正文、履行和导出接口统一校验。

## 24. 合同级临时授权与越权验收（2026-08-31）

- 完成指定合同授权：查看、编辑、导出、法务确认、到期时间、授权说明和撤销状态。
- 授权管理放入既有“权限与审计”模块，避免新增无业务归属的导航项；可选择当前可见合同创建、查看和撤销授权。
- 合同级有效授权可补充角色数据范围访问指定合同，但不能授予系统管理能力。
- 合同详情、正文、附件、AI、审批、收付款和履行接口已统一使用动作级授权判断。
- 验证：后端 Maven compile 通过；前端 npm build 通过。

## 25. 下一完整交付块

- 正式合同数据迁移与口径收口：以 `t_contract_main`、正文版本、附件、收付款计划和履行事项正式表为读取来源，统一台账、详情、工作台和统计口径；保留兼容层和历史数据，不做删除式迁移。

## 26. SQLite 开发验收与运行准备（2026-08-31）

- 产品决策：正式数据库迁移暂停，不再调整正式 SQL 表；当前开发环境继续使用 SQLite。
- 新增 `backend/scripts/backup-sqlite.ps1` 和 `backend/scripts/restore-sqlite.ps1`，用于 SQLite 备份、恢复和 SHA-256 完整性确认。
- 新增 `SQLite开发运行与验收说明.md`，包含启动、备份恢复、SQLite 边界和验收范围。
- 验证：前端 npm build 和后端 Maven compile 通过。Maven test 因本地仓库缺少 Surefire 插件且网络下载被拒绝，未能执行，不属于测试断言失败。

## 27. 下一完整交付块

- SQLite 商用流程回归与异常处理收口：围绕合同创建、审批、签署、履行、变更、授权和越权拒绝编写可重复的接口回归测试；正式数据库迁移继续暂停，等待数据库信息。

## 28. SQLite 商用流程回归与异常处理收口（2026-08-31）

- 正式数据库迁移继续暂停，当前只针对 SQLite 开发库验收。
- 新增 `backend/scripts/smoke-test.ps1`，覆盖健康检查、基础字典、合同创建与正式字段回显、正文、AI、审批、合同级查看授权和编辑拒绝。
- 新增 `SQLite商用验收清单.md`，集中记录流程回归、越权回归和执行方式，不把未联调的外部系统标记为完成。
- 保留已有异常响应：非法状态、缺少必填字段、版本冲突、无权限和范围外合同分别返回业务错误、400、409 或不可见响应。
- 验证：前端 npm build 通过；后端 Maven compile 通过。真实 smoke test 需要后端实例可重启并监听 19090，当前环境的旧 Java 进程权限阻止了稳定重启。

## 29. 下一完整交付块

- 外部系统正式适配前的接口契约整理：在不接入正式数据库的前提下，整理 HR/SSO、文件服务、审批、AI 的请求响应契约、错误码和替换适配边界，等待外部接口文档后联调。
## 30. AI 解析结果确认回填与科技感工作区（2026-09-01）

- 完成 AI 最近一次解析结果读取、显式确认和合同正式字段回填闭环。
- 回填字段包括合同名称、金额、币种、收付方向、签署日期、生效日期和到期日期；标准日期校验失败时不写入。
- 新增接口：`GET /api/contracts/{id}/ai/extract/latest`、`POST /api/contracts/{id}/ai/extract/confirm`；确认动作受合同级权限控制并写入审计日志。
- 新增 `frontend/src/components/AiExtractionReview.vue`，在合同详情内提供字段复核、识别摘要、付款计划建议和确认写入。
- 页面升级为“浅色商用后台 + 深色 AI 决策区”的组合，调整信息密度、字段矩阵、间距、边界和移动端布局，避免只做单个按钮或颜色修改。
- 前端验证：`npm.cmd run build` 通过。后端验证：`mvn.cmd "-Dmaven.repo.local=D:\project\hr-contract\Hrcontract\.m2-local" -DskipTests compile` 通过。
- 未完成：付款计划和履约项的自动回填，需先确定自然语言付款条件的人工确认规则。

### 下一完整交付块

付款计划与履约项确认闭环：把 AI 识别出的付款计划、触发条件、金额、日期和履约节点做成可复核的结构化确认工作区，确认后分别写入收付款计划和履约表，并覆盖重复确认、撤销、权限和审计。
## 31. AI 付款计划与履行项确认闭环（2026-09-01）

- 新增 `GET /api/contracts/{id}/ai/extract/fulfillment-recommendations`，从最近一次 AI 解析结果提取付款计划和履行节点建议。
- 合同详情 AI 工作区新增两类逐条确认清单，支持勾选、编辑名称、金额、日期和说明后一次性提交。
- 后端只写入勾选且通过校验的条目；标准日期、正数金额和必填名称校验失败时整体拒绝，避免产生半成品业务数据。
- 兼容 SQLite/MySQL 计划表增加 AI 来源任务和确认标记，重复确认按合同 + 任务 + 期次更新，确认行为记录操作日志。
- 新增“撤销本次 AI 写入”操作：不物理删除，只将当前任务下仍待处理的 AI 履行项标记为已撤销，并记录撤销审计。
- 未改变用户提供的正式数据库结构；正式表切换仍需在后续迁移模块统一处理。
- 验证：前端 `npm.cmd run build` 通过；后端 `mvn.cmd "-Dmaven.repo.local=D:\project\hr-contract\Hrcontract\.m2-local" -DskipTests compile` 通过。

### 下一完整交付块

正式合同运行时数据切换：将台账、详情、正文、附件、履行收付款、审批、AI、模板、授权和统计从兼容表统一切换到正式 `t_contract_*` 表，保留兼容层和回滚路径。
## 32. 本地登录与会话权限闭环（2026-09-01）

- 新增 `LocalAuthService` 和 SQLite `local_user` 表，默认开发管理员账号用于本地验收。
- 新增登录、会话查询、退出接口；使用 HttpOnly Session，前端请求统一 `credentials: include`。
- `AccessControl` 优先读取 Session 中的用户、组织、部门和角色，旧请求头仅作为兼容联调入口。
- 前端移除 localStorage 登录标志，启动时通过会话接口恢复登录；登录失败、退出和 401 会话失效统一回登录页。
- 不做公开注册，企业账号由管理员维护；IAM/SSO 后续通过适配层接入。
- 跨域配置已开启凭证传递，限定本地前端来源。

### 下一完整交付块

管理员账号与角色维护：在系统管理中完成用户列表、启停用、角色分配、密码重置、组织/部门归属、操作审计和权限验收；不开放公开注册。
## 33. 管理员账号与角色维护（2026-09-01）

- 新增 `AdminUserWorkspace.vue`，与“权限与审计”页面同属系统管理，提供账号目录、状态、角色和组织归属维护。
- 新增用户查询、用户配置更新和密码重置接口；接口统一要求 `TEMPLATE` 管理权限。
- 支持账号启停用、ADMIN/LEGAL/BUSINESS/VIEWER 角色分配、组织 ID、部门 ID 修改和至少 6 位密码重置。
- 账号更新和密码重置写入 `sys_contract_audit_log`；停用账号无法通过本地登录。
- 不开放公开注册，后续接入 HR/IAM 时保留本地账号适配边界。
- 验证：前端 `npm.cmd run build` 通过；后端 Maven compile 通过。

### 下一完整交付块

外部身份目录适配准备：整理 HR/IAM 用户、组织、部门同步契约，将本地账号服务抽象为可替换 Adapter，并完成外部登录/组织数据缺失时的降级和错误提示；不接入未知接口，不改正式数据库表。
## 34. 外部身份目录适配准备（2026-09-02）

- 复用并明确 `DirectoryProvider` 为 HR/IAM 用户、组织、部门适配边界；模拟目录改由 Provider 提供。
- 新增 `IdentityDirectoryService` 与 `GET /api/identity-directory/contract`，输出字段、操作、只读边界和降级规则。
- 外部连接页新增身份目录运行状态：当前为 `LOCAL_FALLBACK`，未把 Mock 当作正式外部接入。
- 约束：合同系统不维护 HR/IAM 主数据；正式接入需提供认证方式、接口地址、字段映射、账号停用/组织变更通知和错误码。
- 验证：待本轮前后端构建完成后记录最终结果。

### 下一完整交付块

身份目录接口联调适配：拿到 HR/IAM 正式文档和测试凭证后，实现真实用户/组织/部门 Provider、登录认证映射、同步缓存、外部不可用降级和联调验收；没有外部资料时不修改为假接口。
## 35. IAM BFF 与门户 OIDC 双入口集成（2026-09-02）

- 新增 IAM Client 和认证编排：`CONTRACT_AUTH_MODE=local` 保持 SQLite 开发降级，切换为 `iam` 后自有登录页调用 IAM 密码登录接口。
- 新增 `/api/auth/oidc/login`、`/api/auth/oidc/callback`，支持 state 校验、授权码后端换 Token、统一 HttpOnly Session。
- 新增 `/api/bff/proxy`，仅代理文档中心和元数据中心路径，并注入 `Authorization` 与 `X-App-Code`，禁止前端直接拿 Token。
- 新增 IAM 配置项：`IAM_BASE_URL`、`IAM_CLIENT_ID`、`IAM_CLIENT_SECRET`、`IAM_APP_CODE`、`IAM_REDIRECT_URI`、`IAM_OIDC_AUTHORIZE_PATH`、`IAM_OIDC_TOKEN_PATH`。
- 当前真实 IAM/OIDC 联调未宣称完成，原因是 client 参数、正式端点和网关授权仍需平台下发。
- IAM 模式下关闭旧的请求头模拟身份后门，只有服务端 Session 才能通过权限校验；本地模式仍保留开发兼容头。
- 验证：前端 `npm.cmd run build` 通过；后端 Maven compile 通过，42 个源文件参与编译。

### 下一完整交付块

IAM/网关真实联调验收：使用平台下发的应用参数完成自有登录、门户回调、Token 失效、退出、文档中心上传下载、元数据查询、403 权限和链路审计全流程验收。
## 36. IAM development integration preparation (2026-09-02)
- Created IAM business application details were recorded: app code `hr_contract_management` and client ID `hr_contract_management-client`.
- Verified the IAM development password-login endpoint is reachable from the development environment.
- Completed the backend integration boundary: IAM password login, OIDC callback/session establishment, and restricted BFF proxy support for document and metadata services.
- Added `IAM_GATEWAY_BASE_URL` to allow the gateway address to differ from the IAM authentication address, and added `backend/.env.iam.example` without secrets.
- OIDC portal entry is intentionally not claimed as tested: it still requires the client secret and confirmed IAM authorize/token endpoint paths. The app remains unpublished.

### Next complete module
IAM real joint verification: configure the secret only in the backend runtime environment, confirm OIDC endpoints, then verify self login, portal callback, session expiry, logout, document gateway authorization and 403/audit handling together.

## 37. RDS MySQL formal schema migration and startup verification (2026-09-02)
- Verified TCP connectivity to the provided RDS MySQL endpoint and started the application using the `mysql` profile against `hr_contract_db`.
- Added the required `flyway-mysql` extension. Flyway safely baselined the pre-existing schema at version `0`, then successfully applied `V1__contract_schema.sql` at version `1`.
- The migration is additive: it creates the contract authority tables and Flyway history table without deleting SQLite or existing RDS data.
- Fixed the local development account table definition from SQLite-only `TEXT UNIQUE` columns to MySQL-compatible bounded `VARCHAR` key columns.
- Verification: MySQL-profile backend started on temporary port `19091`; `GET /actuator/health` returned HTTP 200 with status `UP`.

### Next complete module
Formal-table runtime cutover: move contract ledger, details, attachments, payments, performance, approvals, AI, templates, authorization and statistics from compatibility tables to the authoritative `t_contract_*` tables, with regression verification and a fallback path.

## 42. Formal-table runtime cutover: staged write synchronization (2026-09-02)
- Added a MySQL-only synchronization layer in `MySqlCompatibilitySchema` for正文版本、附件、收付款计划、履约项、AI解析/审查、合同变更、模板及模板版本。
- All mappings populate required formal-table audit fields and convert development string statuses to the formal numeric status values. SQLite remains unchanged.
- Kept compatibility tables as the transition write surface to preserve rollback and avoid disrupting the existing frontend/API contract.
- `mvn -s ..\\maven-settings.xml compile` passed. RDS validation reached the database, but the first run lacked `DB_PASSWORD`; the second run was reclaimed by the command timeout before a complete smoke result could be captured. No full-chain acceptance is claimed.
- Remaining in this same cutover block: formal reads for statistics/approval/authorization and live-field confirmation for `t_contract_object_auth`; these must be completed before calling the module finished.
- A controlled MySQL smoke start on `19091` did not become ready within 30 seconds, so no business test data was written during this attempt.

### Next complete module
Finish formal-table runtime cutover and MySQL regression: switch remaining reads, reconcile authorization/approval fields, run create/detail/content/attachment/fulfillment/AI/template/change/statistics smoke tests, then close the migration block.

## 43. Formal-table runtime cutover: remaining reads switched (2026-09-03)
- 完成迁移块剩余的三处读取切换，关闭本轮 cutover 的 SQLite 侧闭环：
  - 授权：`t_contract_object_auth` 改用 `auth_target_type`/`auth_target_id`（对齐 V1），对外保持 `targetUserId` 字符串契约；旧 `target_user_id` 数据启动时一次性迁移。
  - 审批：`AiWorkflowService.approval()` 改读 `sys_approval_instance`（status/result TINYINT），`submitApproval`/`approve` 仍写兼容表 `approval_mock` 作为过渡写入面。
  - 统计：`StatisticsService` 改读 `t_contract_main`/`t_contract_payment_plan`/`t_contract_perform_item`/`t_contract_ai_extract_task`/`t_contract_ai_review`，状态/风险/范围字段全部走数字枚举映射。
- `FormalSchemaMigration`（SQLite）补齐 `t_contract_ai_extract_task`、`t_contract_ai_review` 两表，新增 `migrateAi()`，并安装运行时 trigger，让兼容表写入持续物化到正式表：收付款计划、履行项、AI 解析/审查、审批（`approval_mock` → `sys_approval_instance`）。同时修正 `migrateApprovals()` 的中文状态字面量为真实英文状态（`APPROVED`/`REJECTED`/`WITHDRAWN`）。
- 验证：`mvn test` 全量通过（7 tests）。新增 `FormalTableCutoverSmokeTest`（6 tests），覆盖迁移版本、审批 trigger→读取、收付款/履行 trigger、AI trigger、授权正式字段读取、统计正式表读取。
- 已知跨库缺口（未擅自消除，留待 DOCX 权威对齐）：MySQL V1 `t_contract_main` 只有 `applicant_id`/`applicant_org_id`、无 `applicant_department_id`，而 SQLite 简化表三者都有；且 SQLite 的 `applicant_id`/`applicant_org_id` 为 TEXT、MySQL 为 BIGINT。MySQL 全量回归前需按 DOCX 权威口径对齐。

### Next complete module
MySQL 全量回归（需运行环境 `DB_PASSWORD`）：在权威 `t_contract_*` 表上跑通 create/detail/content/attachment/fulfillment/AI/template/change/statistics 冒烟，并收口 `applicant_department_id` 字段缺口后关闭迁移块。

## 44. Formal-table runtime cutover: MySQL regression closed (2026-09-03)
- 在 RDS MySQL（`mysql` profile）上完成全量回归，本迁移块关闭：建合同 / 正文版本 / AI 解析 / AI 审查 / 审批提交 / 合同授权 / 授权后查看（VIEWER 10002）/ 越权编辑拒绝（403）/ 统计总览 全部通过。
- 修复两个 MySQL 500，根因是 ISO-8601 字符串与 MySQL `DATETIME` 列格式不匹配（SQLite TEXT 不暴露）：
  - `ContractAuthorizationService`：`auth_start_time`/`create_time`/`revoked_time` 由 `Instant.now().toString()`（`2026-09-03T02:23:08Z`）改为 `dbNow()`（`yyyy-MM-dd HH:mm:ss`，UTC）；`allowed()` 过期比较同步改用 `dbNow()`。
  - `AiWorkflowService.approval()`：`Instant.parse(update_time)` 改为 `parseInstant()`，先按 ISO-8601 解析、失败回退 `Timestamp.valueOf().toInstant()`，兼容 MySQL `DATETIME` 读回格式。
- 验证：`mvn test` 全量 7 tests 通过（SQLite 本地路径未回归）；MySQL 实机回归接口全绿。
- 跨库字段缺口已按 DOCX 权威核对：DOCX `t_contract_main` 只有 `applicant_id`（合同发起人 ID）+ `applicant_org_id`（描述「发起部门 ID」），**没有** `applicant_department_id`。因此 MySQL V1 正确，SQLite 多出的 `applicant_department_id` 是非权威字段，`StatisticsService` 的「部门」数据权限 `c.applicant_department_id` 引用的是非权威列。当前无角色使用「部门」数据权限，不阻塞，留待后续把部门范围统一到 `applicant_org_id` 并清理 SQLite 冗余列。

### Next complete module
权限与审计真实数据范围收口：把「部门」数据范围统一映射到 DOCX 权威的 `applicant_org_id`，清理 SQLite 冗余 `applicant_department_id`，并接入真实 HR/SSO 组织数据。

## 41. Dify real output schema mapping (2026-09-02)
- Reviewed the actual `extract-v1` output: `basic_info`, `payment_plans`, `missing_fields`, confidence and evidence fields are present. Payment plans use `plan_amount`, `plan_due_date`, and numeric `payment_direction`.
- Reviewed the actual `review-v1` output: the risk report is wrapped by a `result` object and contains the overall level, counters, detailed risks and legal-confirmation flags.
- Updated the provider mapping to unwrap the review `result` envelope, convert payment direction `1/2` to receive/pay, and use `evidence` as the fallback payment condition.
- This preserves the existing human-confirmation boundary: AI results are displayed and only write back after user confirmation.
- Verification: backend Maven compile passed.

### Next complete module
Formal-table runtime cutover: move contract ledger, details, attachments, payments, performance, approvals, AI, templates, authorization and statistics from compatibility tables to the authoritative `t_contract_*` tables, with regression verification and a fallback path.

## 38. Dify Workflow API alignment and real call verification (2026-09-02)
- Confirmed from the Dify API reference that both Contract Extraction and Legal Risk Review are Workflow applications.
- Updated the provider so both capabilities use `POST /workflows/run` with `response_mode=streaming`; extraction no longer uses the chat-only `/chat-messages` endpoint.
- Updated workflow result handling to read `workflow_finished.data.outputs`, supporting either a direct output object or a JSON string in `outputs.result`.
- Backend compilation passed.
- Real calls from this development machine, using the documented endpoint, `Authorization: Bearer` and streaming request format, returned `405 text/html` for both applications. This is recorded as a gateway/routing issue; no Dify result mapping has been claimed as validated.

### Next complete module
Formal-table runtime cutover remains the next internal delivery. Dify real-result acceptance resumes when the platform exposes the workflow API route to the contract-system runtime.

## 39. Dify actual application-type and gateway diagnosis (2026-09-02)
- Verified the real Dify service route is `http://metric-dify.qd-ecs.com:8088/v1`; the prior port-80 address was handled by a different upstream route.
- Dify returned `not_workflow_app` for the Contract Extraction API key on `/workflows/run`; extraction was corrected to its actual chat endpoint `/chat-messages`.
- Legal Risk Review remains a Workflow application at `/workflows/run`; the provider retains SSE streaming and Workflow output parsing.
- Dify parameter APIs confirmed that all contract-system input names match the currently published app definitions, including `contract_text`, `contract_id`, `contract_type`, `content_version_id`, `schema_version`, and review-specific fields.
- Real execution is not yet accepted: legal-review blocking calls reached Dify but received a gateway `504` after about 60 seconds, and extraction streaming did not finish within 120 seconds. This is a Dify/model/gateway execution-time issue, not a missing request field.
- Verification: backend Maven compile passed.

### Next complete module
Formal-table runtime cutover remains the next internal delivery. Dify acceptance resumes after the Dify platform confirms the running task status and increases or bypasses the gateway timeout for API streaming.

## 40. Dify execution outcome confirmed (2026-09-02)
- Dify execution logs confirmed both contract-system calls completed with `SUCCESS`.
- Legal Risk Review completed in approximately 152.280 seconds. The prior HTTP 504 was caused by using blocking mode through a gateway with a shorter timeout, not by a workflow failure.
- Contract Extraction chat execution also completed successfully. The client-side test timeout was shorter than the actual streaming completion time.
- Resolution: retain SSE streaming in the contract system and configure `DIFY_BASE_URL=http://metric-dify.qd-ecs.com:8088/v1`. No Dify workflow redesign or missing-input change is required.

### Next complete module
Formal-table runtime cutover: move contract ledger, details, attachments, payments, performance, approvals, AI, templates, authorization and statistics from compatibility tables to the authoritative `t_contract_*` tables, with regression verification and a fallback path.

## 42. Dify timeout and answer-fence hardening (2026-09-02)
- Confirmed the SSE streaming and field mapping were already in place from the prior Dify blocks; no rewrite was needed.
- Raised the Dify read timeout from 180s to 300s so the ~152s legal review and slower extraction do not drop the SSE stream mid-run.
- Added markdown-fence stripping to the stored extraction answer so a ```json ...``` wrapped chat answer still parses instead of failing `readTree`.
- Verification: `mvn -s ../maven-settings.xml clean compile` passed (42 sources).
- Real Dify acceptance still requires `DIFY_BASE_URL=http://metric-dify.qd-ecs.com:8088/v1` plus the two API keys set in the backend runtime environment; keys are never exchanged through chat.

### Next complete module
Formal-table runtime cutover (unchanged): move ledger/detail/attachment/payment/performance/approval/AI/template/authorization/statistics reads from compatibility tables to the authoritative `t_contract_*` tables with regression and a fallback path.

## 45. 登录与会话/界面可见性修复（2026-09-03）

- 登录 401 修复：`LocalAuthService.init()` 从「仅当 `local_user` 为空才写入 admin」改为幂等校正，缺失则插入、存在但 `password_hash` 非 `hash("admin123")` 则重置并 `enabled=1`。历史 SQLite 库里残留的旧密码 admin 行不再导致「账号或密码不正确」。
- 刷新退出修复：`frontend/src/App.vue` 的 `api` 由 `'http://127.0.0.1:19090'` 改为 `''`，请求改走 Vite `/api` 代理（同源）。根因是页面运行于 `http://localhost:5173`、直连 `127.0.0.1:19090` 属跨站，HttpOnly 会话 cookie（SameSite=Lax）不会随跨站 fetch 回传，后端每次当新会话 → 401 退出。
- 界面可见性修复：`frontend/src/styles.css` 清除两条压制规则——侧栏 `background:#fff !important` 与 `workspace-banner{display:none}`；现为深色 L 形框架（深色侧栏 `#0f172a` + 深色顶栏）+ 亮色内容 + 靛蓝渐变大横条恢复显示，数字等宽、单一靛蓝强调色。
- 验证：前端 `npm run build`（vue-tsc + vite）通过；后端 `mvn -o compile` 通过。后端需重启一次使种子校正与代理生效（前端 Vite 热更新即时生效）。

## 46. 模板、简单审批与签署生效闭环（2026-09-08）

- 模板起草：新建合同选用已发布模板后，系统绑定实际 `contract_template_version` 版本并生成独立合同正文版本；模板后续变更不会覆盖既有合同。当前模板正文继续使用本地兼容表保存，后续可将同一模板版本关联公司文档中心 `fileId`，不改变合同生成链路。
- 简单审批：补齐提交、模拟通过、模拟驳回和撤回。提交时冻结当前正文版本和内容哈希，递增审批轮次，并写入 `t_contract_approval_record`；审批中合同的草稿编辑和正文保存由后端拒绝。
- 签署生效：签署文件上传时绑定当前正文版本；确认生效时校验审批已通过、当前正文版本未偏离送审版本、签署附件也绑定该版本，随后合同从“待签署”进入“履行中”。
- 追溯：模板绑定、送审、审批结果和生效均写入合同操作审计。审批状态与合同生命周期状态独立保存。
- 验证：后端 `mvn test` 通过（7 tests）；前端 `npm.cmd run build` 通过。

### 下一完整交付块

履约与收付款：将计划与实际收付款分开，支持履约节点状态历史、交付/验收/付款凭证附件、逾期与异常待办，并保持与已签署正文版本和合同权限的一致性。

## 47. 履约与收付款闭环（2026-09-08）

- 实际收付款：新增 `contract_payment_record` 兼容写入面及 `t_contract_payment_record` 同步。实际金额、日期、凭证附件和说明独立于收付款计划保存；同一计划允许分次登记，累计金额不能超过计划金额，计划状态由累计实际金额派生为待处理、部分完成或已完成。
- 履约节点：增加处理中、延期、异常、取消状态；延期和异常必须填写原因。每次状态变化新增 `contract_fulfillment_event`，同时保留合同操作审计，避免覆盖历史处理过程。
- 凭证关联：实际收付款可选择当前合同的已有附件作为凭证，后续接公司文档中心时仍只替换文件标识来源，不改变收付款记录关系。
- 页面：履约与收付款工作区支持新增计划、维护节点状态、登记多笔实际收付款和查看当前计划的实际记录。
- 约束：只有履行中合同允许处理履约和收付款；实际收付款、节点状态和凭证均经后端合同范围与 `FULFILL` 权限校验。
- 验证：后端 `mvn clean test` 通过（7 tests）；前端 `npm.cmd run build` 通过。未启动或重启应用服务。

### 下一完整交付块

合同变更、补充协议、归档与分析：完成变更独立审批及版本切换，增加完成/终止后的归档只读保护，并让分析口径覆盖合同、履约、收付款和风险状态。

## 48. 风险与到期待办（2026-09-08）

- 新增工作台待办接口 `GET /api/attention-items`，统一汇总履行中合同 30 天内到期、逾期收付款计划、延期/异常/逾期履约节点，以及 AI 审查高风险合同。
- 待办按当前用户的数据范围和合同类型权限过滤，归档合同不再产生待办，避免越权查看和无效提醒。
- 工作台新增“风险与到期待办”列表，并将通知数量改为真实待办数量；点击合同类待办进入合同详情，履约类待办直接进入履约处理视图。
- 验证：后端 `mvn test` 通过（7/7）；前端 `npm run build` 通过。未启动、停止或重启服务。

### 下一完整交付候选方案

待办处理闭环与审计告警：为每条待办增加处理状态、处理意见、处理人和处理时间；支持已处理/忽略/重新打开，保留全程审计；对逾期和高风险增加可配置的升级规则，并在工作台和合同详情展示处理轨迹。导出能力继续复用现有权限范围，避免导出越权数据。

## 49. 待办处理闭环与审计告警（2026-09-08）

- 待办处理：新增待办处置和历史记录表，以“待办类别 + 合同 + 到期日”稳定标识关联运行时待办；支持已处理、忽略和重新打开，处理与忽略必须填写意见。
- 权限与审计：待办展示继续按合同数据范围和类型权限过滤；处理操作要求合同编辑权限，同时写入合同操作记录和全局审计日志，保留处理人、时间和意见。
- 工作台：未处理待办才计入通知数量；列表显示当前处理状态、处理轨迹，并提供处理、忽略、重新打开入口。合同详情的操作记录同步显示待办处理轨迹。
- 升级规则：高优先级、到期后超过 3 天仍未处理的待办标记为“已升级”。当前仅在系统内标记，不会伪造外部通知；后续公司通知接口到位后可直接接入推送。
- 数据库：新增 MySQL Flyway 迁移 `V3__attention_item_handling.sql`，本地 SQLite 运行时同步建表。
- 验证：后端 `mvn -s ../maven-settings.xml clean test` 通过（7/7）；前端 `npm.cmd run build` 通过。未启动、停止或重启服务。

## 50. 分析导出与门户集成预留（2026-09-08）

- 分析导出：新增 `GET /api/exports/analysis`，浏览器直接下载 UTF-8 CSV，不额外落地临时文件或占用文件中心。内容覆盖统计汇总、可见合同台账、收付款计划、履约节点，以及风险与待办处理状态。
- 权限与审计：导出接口强制校验 `EXPORT` 权限；数据范围与合同类型过滤沿用当前用户权限，导出完成写入全局审计，记录导出人、合同数量和待办数量。
- 工作台：有导出权限的用户可从“风险与到期待办”区域直接下载当前权限范围的分析报表。
- 门户预留：OIDC 登录入口支持 `returnTo` 页面参数，回调后可回到工作台、台账、履约、基础配置或账号管理。参数采用服务端白名单校验，拒绝任意外部 URL，避免开放重定向风险。
- 约束：未连接公司门户、IAM 或通知接口；真实接口文档和密钥到位后，仅需替换配置及进行联调，不改变当前业务导出和权限边界。
- 验证：后端 `mvn -s ../maven-settings.xml clean test` 通过（7/7）；前端 `npm.cmd run build` 通过。未启动、停止或重启服务。

## 51. 新建合同流程重构（2026-09-09）

- 入口：统一使用“新建合同”进入创建流程，避免“发起合同”和“创建合同”造成职责重复。
- 来源分流：创建第一步区分“标准模板起草”和“对方合同登记”两条真实业务路径。
- 模板约束：标准模板起草只能选择已发布模板，并固化所选模板版本；模板变更不覆盖已经创建的合同正文。
- 对方合同：对方合同先保存基础信息草稿，再进入合同详情上传 Word/PDF 原始文件并继续 AI 解析，避免在创建页伪造文件处理能力。
- 页面流程：步骤调整为选择来源、填写合同信息、确认合同草稿；保存按钮明确为“保存草稿并查看”，保存后进入合同详情继续处理。
- 验证：前端 `npm.cmd run build` 通过；本次未启动、停止或重启任何服务。

## 52. 变更、归档与分析闭环（2026-09-09）

- 变更流程：保留现有变更与补充协议入口，统一覆盖草稿、提交审批、通过、驳回、撤回和确认生效；审批通过前不切换合同当前正文，生效后才切换版本。
- 审批记录：修复变更审批写入正式审批记录表时缺少创建人和创建时间的问题，兼容本地 SQLite 已存在的正式表结构。
- 归档规则：仅已完成或已终止合同允许归档；归档后后端统一拒绝正文、附件、履约、收付款和变更写操作，详情页补充明确的只读提示。
- 分析口径：复用现有权限范围，统计合同生命周期、合同金额、收付款、履约、AI 风险、变更和归档数量；工作台保留分析导出入口，不新增重复的统计菜单。
- 验证：后端 `mvn -s ../maven-settings.xml clean test` 通过（7/7）；前端 `npm.cmd run build` 通过。未启动、停止或重启任何服务。

## 54. 合同处理页面与流程重构（2026-09-09）

- 详情页按真实工作任务拆分为“概览、正文与文件、审批与签署、履约与收付款、变更与归档”五个区域；不再把正文、AI 审查、审批、履约和变更堆叠在同一屏内。
- 审批与签署独立承载送审、模拟审批、撤回、驳回、审查结果和签署生效；正文与文件独立承载版本、附件、正式字段和 AI 解析，现有接口与状态约束未改变。
- 变更与归档改为合同详情内的任务入口：明确变更审批生效前不覆盖正文、归档后只读，以及归档的可执行条件；合同级授权继续作为受权限控制的辅助操作，不与主流程抢占主按钮。
- 视觉统一为中性白灰背景、单一蓝色主操作、清晰卡片边界和固定的任务导航；删除工作台“用 CSS 覆盖按钮文案”的做法，详情页在窄屏下保留横向可滚动导航。
- 验证：前端 `npm.cmd run build` 通过。未启动、停止或重启任何服务。

## 56. 三条合同创建路径闭环（2026-09-09）
- 入口统一为“新建合同”，弹窗明确区分三条真实业务路径：模板起草、上传待审合同、登记已签合同；不再以“发起合同 / 创建合同”等不同名称表达同一入口。
- 上传待审合同：创建草稿后上传 Word、PDF 或扫描件原件，页面明确提示“文件已保存，下一步在合同详情发起解析”；不会自动宣称已完成 AI 识别，也不会提前进入履约。
- 登记已签合同：上传合同原件时强制补充签署日期和生效日期；新增 `POST /api/contracts/{id}/register-signed`，校验原件属于当前合同、合同仍为草稿、生效日期不早于签署日期，随后转为“履行中”。该动作不创建或伪造签前审批记录，并写入独立操作审计日志。
- 当前交易对方仍使用本地可选主体；后续接入 CRM/MDM 后，应仅保存外部主体 ID、来源与名称快照，未匹配主体只能作为待主数据确认项，不能由合同系统自动创建正式客户。
- 验证：前端 `npm.cmd run build` 通过；后端 `mvn -s ../maven-settings.xml test` 通过（7 项测试）。未启动、停止或重启任何服务。

## 57. 合同创建交互去向导化（2026-09-09）
- 删除模板起草页面的“创建流程 / STEP 1/3 / 上一步 / 下一步 / 确认”向导式交互；创建来源仅在“新建合同”小弹窗中选择一次，不再占用完整页面。
- 模板起草改为单页完整表单：同屏维护合同名称、类型、我方主体、对方主体、金额、期限、收付款方向、模板和备注；底部仅保留取消与“生成合同初稿”，不再发生页内分步切换。
- 上传待审合同和登记已签合同继续使用各自的单页文件+信息表单；已签登记成功后直接定位“履约与收付款”，待审上传后定位“正文与文件”。
- 合同详情不增加“下一步”提示条或流程引导页，仅展示合同状态、详情分区和当前状态允许的实际操作按钮。
- 工作台创建入口统一调整为“新建合同”。
- 验证：前端 `npm.cmd run build` 通过。未启动、停止或重启任何服务。

## 58. 文件解析结果核对与主体确认（2026-09-09）
- 上传待审合同、登记已签合同改为“左侧文件 / 右侧 AI 解析结果核对”布局：文件上传前不展示空白业务表单；文本提取和 AI 解析完成后，合同名称、合同类型、交易对方、金额、币种、收付款方向和日期自动填入右侧，用户只需核对或修改。
- 文件区支持点击选择、拖拽放入、显示已选文件卡片和移除重选；新增“提取文本并 AI 解析”操作。DOC/DOCX/PDF 通过 Apache Tika 提取可读取文本，随后复用既有 AI 提取能力；JPG/PNG 和扫描件在未接入 OCR 前仅归档并明确提示不能自动读字，不伪造解析成功。
- 新增文件导入草稿与 `POST /api/contracts/{id}/ai/extract-file`：先创建受控的待解析草稿、保存原件、从附件提取文本并创建正文版本，再发起 AI 提取任务。原件、提取文本、AI 原始结果与最终合同字段分开保存。
- 解析确认新增合同类型、交易对方确认：合同名称、金额、日期等确认后写入合同主表；确认的交易对方写入合同主体引用（外部 ID/来源/名称快照）。AI 无法唯一匹配的主体保留为“待主数据确认”，不会自动创建正式客户或供应商。
- 已签路径在确认字段后仍由独立“登记已签”动作校验签署日、生效日和原件归属，再进入履约；待审路径仅保存待审草稿，不跳过法审和审批。
- 验证：前端 `npm.cmd run build` 通过；后端 `mvn -s ../maven-settings.xml test` 通过（7 项测试）。未启动、停止或重启任何服务。

## 59. 基础配置统一与合同台账重构（2026-09-09）
- 主体主数据：新增 `contract_party_master` 本地配置表及 `/api/config/parties` 查询、新增、启停接口；原 `/api/trade-parties` 改为读取统一配置，起草、上传解析、AI 确认共用同一主体来源。
- 基础配置：新增“对方主体”维护区，支持按名称、主体编码、统一社会信用代码搜索，支持新增客户、供应商、员工、组织及其他主体；新增主体后立即可在合同选择中使用。
- 起草表单：明确必填项和字段用途，区分系统带出、选择和手工输入；增加“是否涉及金额”联动，金额、币种、收付款方向仅在金额合同中展示并按条件必填；我方主体明确为当前组织带出并预留权限切换边界。
- 合同台账：删除左侧分类树、复选框和独立操作列，改为全宽筛选区与合同列表；支持关键词、合同类型、状态、对方主体筛选，点击整行进入详情，导出当前筛选结果。
- 验证：前端 `npm.cmd run build` 通过；后端 `mvn -s ../maven-settings.xml -q -DskipTests compile` 通过；未启动、停止或重启任何服务。

## 55. 已签与纸质合同登记闭环（2026-09-09）

- 新增“新建或登记合同”入口，先区分“从模板起草”和“登记已签/纸质合同”两条业务路径，避免把所有合同都当作模板草稿。
- 已签/纸质路径要求在登记时选择本地 Word、PDF、JPG 或 PNG 合同原件；保存时先创建合同，再通过现有附件接口以“合同原件”类型上传。创建或上传任一步失败都会明确报错，不会伪造成功结果。
- 上传成功后自动进入该合同的“正文与文件”区域，用户可以在同一合同下继续发起解析、核对解析结果、人工确认字段、发起法律审查和审批。
- 本次仍使用本地文件存储；未调用公司文档中心，不会写入任何公司文件数据。后续仅需替换文件存储适配器，不改变“登记 → 原件 → 解析 → 人工确认”的业务链。
- 验证：前端 `npm.cmd run build` 通过。未启动、停止或重启任何服务。
## 60. AI 能力配置页面（2026-09-10）
- 配置入口：在“系统管理 → 基础配置”新增 AI 能力配置区，参考招聘系统 AI 中心的能力清单和 API 密钥配置方式，并保留 ContractManage-main 的 AI 适配层边界。
- 服务配置：支持本地 Mock、Dify、DeepSeek/公司 AI 网关的目标提供方、API 地址、模型、密钥掩码、启用状态和连接测试；密钥只写服务端，不回显前端。
- 工作流配置：新增 AI 辅助起草、合同信息解析、法律审查、履约信息提取四类工作流标识，页面明确每个工作流的使用场景。
- 运行状态：页面同时显示已保存的目标配置和当前运行环境实际提供方；当前默认仍是 Mock，未宣称已经接入 Dify 或 DeepSeek。
- 接口与数据：新增 `contract_ai_config` 配置表、`GET/PUT /api/ai/config` 和 `POST /api/ai/config/test`；新增 MySQL 迁移 `V5__contract_ai_config.sql`。
- 验证：前端 `npm.cmd run build` 通过；后端 `mvn -s ../maven-settings.xml -q -DskipTests compile` 通过；未启动或重启服务。
## 61. AI 配置驱动运行适配器（2026-09-10）
- 运行链路：新增 `ConfigurableAiProvider`，合同 AI 请求统一从 `contract_ai_config` 读取当前提供方、启用状态、API 地址、密钥和工作流 ID，不再由合同业务直接依赖环境变量选择 Mock/Dify。
- Mock 行为：本地 Mock 仍保留，用于无真实 AI 服务时验证合同页面和流程；未启用、缺少地址、密钥或工作流 ID 时明确返回失败原因，不伪造 AI 成功结果。
- Dify 行为：支持按合同解析、法律审查、AI 起草、履约提取能力读取对应工作流配置，服务端通过统一适配层调用，前端不接触密钥。
- 配置模块：页面保存的配置会影响后续运行时 AI 请求；页面同时显示配置提供方和当前数据库运行模式，避免把 Mock 误显示为真实 AI。
- 验证：后端 `mvn -s ../maven-settings.xml -q test` 通过（7 项）；前端 `npm.cmd run build` 通过；未启动或重启服务。
## 62. AI 配置真实调用与辅助起草闭环（2026-09-10）
- 真实配置生效：`ConfigurableAiProvider` 每次请求读取数据库中的启用状态、提供方、地址、模型、密钥和能力工作流 ID；业务页面只调用合同系统后端，不直连 Dify 或 DeepSeek。
- 提供方适配：Dify 统一使用配置的 Workflow ID 调用 `/workflows/run`；DeepSeek/公司 AI 网关使用兼容 OpenAI 的 `/chat/completions`；解析、审查、起草和履约提取统一转换为现有业务结果结构。
- 配置测试：`POST /api/ai/config/test` 会执行真实连接检查，失败时记录真实错误摘要；未启用、缺少地址、密钥或工作流 ID 时明确失败。
- 密钥安全：新增 `AiSecretCrypto`，API Key 使用 AES-GCM 加密后保存，接口和页面不回显明文；支持通过 `CONTRACT_AI_CONFIG_KEY` 设置部署密钥。
- AI 起草入口：新增 `POST /api/ai/draft`，合同起草页可提交合同名称、类型、主体、金额、期限和特殊要求，返回 AI 草稿正文；用户可直接编辑，只有点击保存后才写入合同正文版本，并保留人工确认边界。
- 审计：新增 `ai_call_log` 和 MySQL Flyway `V6__contract_ai_call_log.sql`，记录合同、能力、提供方、成功/失败和错误摘要，不记录 API Key 或完整敏感请求内容。
- 兼容处理：旧的静态 Mock/Dify Bean 改为显式 legacy 模式，默认避免与数据库驱动适配器重复注入。
- 页面重做：参考招聘系统的 AI 中心与 API 密钥管理方式，拆分为“提供方凭证”和“合同 AI 能力”两块；起草、解析、审查、履约提取分别拥有自己的工作流/模型配置、启停、保存和测试操作，不再使用页面底部统一操作栏。
- 数据与接口：新增四个能力级启用字段和 `POST /api/ai/config/test/{capability}`，测试按钮会调用对应能力的真实适配器；DeepSeek 不显示 Dify 工作流字段，Dify 才显示工作流 ID。
- 验证：前端 `npm.cmd run build` 通过；后端 `mvn -s ..\maven-settings.xml -DskipTests package` 和 `mvn -s ..\maven-settings.xml test` 均通过（7 项测试）；未启动或重启任何服务。
### 下一完整交付块
将 AI 调用审计展示到合同详情和系统管理，补齐 AI 权限细分（配置、起草、解析、审查、结果确认），并用真实 Dify/DeepSeek 配置完成一次端到端联调。
