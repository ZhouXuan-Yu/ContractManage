spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://rm-8vb7m858r8wt3b10hjo.mysql.zhangbei.rds.aliyuncs.com:3306/hr_contract_db?useSSL=true&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai&characterEncoding=utf8}
    username: ${DB_USERNAME:hr_contract}
    password: ${DB_PASSWORD:}
    driver-class-name: com.mysql.cj.jdbc.Driver
  flyway:
    # Versioned, additive schema baseline for the dedicated contract database.
    enabled: true
    baseline-on-migrate: true
    baseline-version: 0
    locations: classpath:db/migration
