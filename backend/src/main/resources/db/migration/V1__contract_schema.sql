CREATE TABLE IF NOT EXISTS t_contract_main (id BIGINT PRIMARY KEY, contract_no VARCHAR(64) NOT NULL, contract_name VARCHAR(255) NOT NULL, category_big_id BIGINT, category_type_id BIGINT, category_sub_id BIGINT, category_version_id BIGINT NOT NULL, contract_status TINYINT NOT NULL DEFAULT 0, our_role TINYINT, our_subject_source_type TINYINT, our_subject_external_id VARCHAR(128), payment_direction TINYINT NOT NULL, total_amount DECIMAL(18,2), currency VARCHAR(8) DEFAULT 'CNY', sign_date DATE, effective_date DATE, expire_date DATE, source_type TINYINT NOT NULL, template_version_id BIGINT, origin_file_id BIGINT, signed_file_id BIGINT, current_content_version_id BIGINT NOT NULL, ai_extract_status TINYINT NOT NULL DEFAULT 0, ai_review_status TINYINT NOT NULL DEFAULT 0, current_extract_task_id BIGINT, current_review_id BIGINT, risk_level TINYINT, approval_status TINYINT NOT NULL DEFAULT 0, approval_instance_id BIGINT, approval_generation INT NOT NULL DEFAULT 1, approval_sync_time DATETIME, applicant_id BIGINT NOT NULL, applicant_org_id BIGINT, void_time DATETIME, void_by BIGINT, void_reason VARCHAR(500), remark VARCHAR(500), create_by BIGINT NOT NULL, create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, update_by BIGINT, update_time DATETIME NULL ON UPDATE CURRENT_TIMESTAMP, deleted TINYINT NOT NULL DEFAULT 0, UNIQUE KEY uk_contract_no(contract_no), KEY idx_category_version_id(category_version_id), KEY idx_contract_status(contract_status), KEY idx_approval_status(approval_status), KEY idx_applicant_id(applicant_id));
CREATE TABLE IF NOT EXISTS t_contract_party (id BIGINT PRIMARY KEY, contract_id BIGINT NOT NULL, party_source_type TINYINT NOT NULL, external_party_id VARCHAR(128) NOT NULL, party_role VARCHAR(64) NOT NULL, is_our_party TINYINT NOT NULL DEFAULT 0, our_subject_id VARCHAR(128), display_name VARCHAR(255), party_nature TINYINT NOT NULL, snapshot_id BIGINT, sort_num INT NOT NULL DEFAULT 0, create_by BIGINT NOT NULL, create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, update_by BIGINT, update_time DATETIME NULL ON UPDATE CURRENT_TIMESTAMP, deleted TINYINT NOT NULL DEFAULT 0, KEY idx_contract_id(contract_id), KEY idx_external_party_id(party_source_type,external_party_id));
CREATE TABLE IF NOT EXISTS t_contract_content_version (id BIGINT PRIMARY KEY, contract_id BIGINT NOT NULL, version_no INT NOT NULL DEFAULT 1, content MEDIUMTEXT, content_hash VARCHAR(64) NOT NULL, version_source TINYINT NOT NULL, source_template_version_id BIGINT, origin_file_text MEDIUMTEXT, is_current TINYINT NOT NULL DEFAULT 0, submit_approval_flag TINYINT NOT NULL DEFAULT 0, sign_flag TINYINT NOT NULL DEFAULT 0, create_by BIGINT NOT NULL, create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, update_by BIGINT, update_time DATETIME NULL ON UPDATE CURRENT_TIMESTAMP, deleted TINYINT NOT NULL DEFAULT 0, KEY idx_contract_id(contract_id), KEY idx_is_current(is_current), UNIQUE KEY uk_contract_version(contract_id,version_no));
CREATE TABLE IF NOT EXISTS t_contract_clause (id BIGINT PRIMARY KEY, contract_id BIGINT NOT NULL, clause_no VARCHAR(64), clause_location VARCHAR(128), clause_type TINYINT NOT NULL, clause_title VARCHAR(255), clause_content TEXT, clause_source TINYINT NOT NULL, source_extract_task_id BIGINT, content_version_id BIGINT, ai_result_id BIGINT, confidence DECIMAL(5,2), evidence_text TEXT, is_confirmed TINYINT NOT NULL DEFAULT 0, sort_num INT NOT NULL DEFAULT 0, create_by BIGINT NOT NULL, create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, update_by BIGINT, update_time DATETIME NULL ON UPDATE CURRENT_TIMESTAMP, deleted TINYINT NOT NULL DEFAULT 0, KEY idx_contract_id(contract_id), KEY idx_clause_type(clause_type), KEY idx_source_extract_task_id(source_extract_task_id), KEY idx_content_version_id(content_version_id));
CREATE TABLE IF NOT EXISTS t_contract_item (id BIGINT PRIMARY KEY, contract_id BIGINT NOT NULL, item_name VARCHAR(255) NOT NULL, item_type TINYINT NOT NULL, spec VARCHAR(256), quantity DECIMAL(18,2), unit VARCHAR(16), unit_price DECIMAL(18,2), subtotal_amount DECIMAL(18,2), item_desc TEXT, source_extract_task_id BIGINT, content_version_id BIGINT, confidence DECIMAL(5,2), evidence_text TEXT, is_confirmed TINYINT NOT NULL DEFAULT 0, sort_num INT NOT NULL DEFAULT 0, create_by BIGINT NOT NULL, create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, update_by BIGINT, update_time DATETIME NULL ON UPDATE CURRENT_TIMESTAMP, deleted TINYINT NOT NULL DEFAULT 0, KEY idx_contract_id(contract_id), KEY idx_source_extract_task_id(source_extract_task_id), KEY idx_content_version_id(content_version_id));
CREATE TABLE IF NOT EXISTS t_contract_payment_plan (id BIGINT PRIMARY KEY, contract_id BIGINT NOT NULL, phase_no INT NOT NULL DEFAULT 1, plan_name VARCHAR(255), payment_direction TINYINT NOT NULL, plan_amount DECIMAL(18,2) NOT NULL DEFAULT 0, trigger_condition VARCHAR(500), payment_condition_type TINYINT, plan_due_date DATE, paid_amount DECIMAL(18,2), paid_date DATE, pay_status TINYINT NOT NULL DEFAULT 0, is_overdue TINYINT NOT NULL DEFAULT 0, source_extract_task_id BIGINT, content_version_id BIGINT, confidence DECIMAL(5,2), evidence_text TEXT, is_confirmed TINYINT NOT NULL DEFAULT 0, reminder_rule JSON, remark VARCHAR(500), sort_num INT NOT NULL DEFAULT 0, create_by BIGINT NOT NULL, create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, update_by BIGINT, update_time DATETIME NULL ON UPDATE CURRENT_TIMESTAMP, deleted TINYINT NOT NULL DEFAULT 0, KEY idx_contract_id(contract_id), KEY idx_pay_status(pay_status), KEY idx_plan_due_date(plan_due_date));
CREATE TABLE IF NOT EXISTS t_contract_payment_record (id BIGINT PRIMARY KEY, payment_plan_id BIGINT NOT NULL, payment_amount DECIMAL(18,2) NOT NULL DEFAULT 0, payment_date DATE, payment_status TINYINT NOT NULL, reverse_of_id BIGINT, reverse_reason VARCHAR(500), voucher_file_id BIGINT, remark VARCHAR(500), create_by BIGINT NOT NULL, create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, update_by BIGINT, update_time DATETIME NULL ON UPDATE CURRENT_TIMESTAMP, deleted TINYINT NOT NULL DEFAULT 0, KEY idx_payment_plan_id(payment_plan_id), KEY idx_reverse_of_id(reverse_of_id));
CREATE TABLE IF NOT EXISTS t_contract_ai_extract_task (id BIGINT PRIMARY KEY, contract_id BIGINT NOT NULL, task_id VARCHAR(128) NOT NULL, request_version INT NOT NULL, content_hash VARCHAR(64) NOT NULL, task_status TINYINT NOT NULL, auto_retry_num TINYINT NOT NULL DEFAULT 0, manual_retry_num TINYINT NOT NULL DEFAULT 0, schema_version VARCHAR(16), result_confirm_status TINYINT NOT NULL DEFAULT 0, confirm_by BIGINT, confirm_time DATETIME, task_provider VARCHAR(32), error_code VARCHAR(64), error_message VARCHAR(500), request_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, start_time DATETIME, finish_time DATETIME, raw_result_json MEDIUMTEXT, create_by BIGINT NOT NULL, create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, update_by BIGINT, update_time DATETIME NULL ON UPDATE CURRENT_TIMESTAMP, deleted TINYINT NOT NULL DEFAULT 0, KEY idx_contract_id(contract_id), KEY idx_task_status(task_status), KEY idx_task_id(task_id));
CREATE TABLE IF NOT EXISTS t_contract_ai_review (id BIGINT PRIMARY KEY, contract_id BIGINT NOT NULL, ai_task_id VARCHAR(128) NOT NULL, bind_extract_task_id BIGINT, provider_task_id VARCHAR(128), review_version INT NOT NULL DEFAULT 1, review_view TINYINT NOT NULL, task_status TINYINT NOT NULL, request_version INT NOT NULL, request_content_version_id BIGINT NOT NULL, confirmed_content_version_id BIGINT, request_hash VARCHAR(64) NOT NULL, schema_version VARCHAR(16), manual_retry_num TINYINT NOT NULL DEFAULT 0, task_provider VARCHAR(32), raw_result_json MEDIUMTEXT, manual_risk_items_json MEDIUMTEXT, manual_summary TEXT, overall_risk_level TINYINT, risk_stat_json JSON, risk_items_json MEDIUMTEXT, review_summary TEXT, confirmed_status TINYINT NOT NULL DEFAULT 0, confirm_by BIGINT, confirm_time DATETIME, last_edit_by BIGINT, last_edit_time DATETIME, error_desc VARCHAR(500), create_by BIGINT NOT NULL, create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, update_by BIGINT, update_time DATETIME NULL ON UPDATE CURRENT_TIMESTAMP, deleted TINYINT NOT NULL DEFAULT 0, UNIQUE KEY uk_contract_review_version(contract_id,review_version), KEY idx_contract_id(contract_id), KEY idx_task_status(task_status));
CREATE TABLE IF NOT EXISTS t_contract_attachment (id BIGINT PRIMARY KEY, contract_id BIGINT NOT NULL, change_id BIGINT, attach_type TINYINT NOT NULL, biz_attach_type TINYINT, attach_version INT NOT NULL DEFAULT 1, content_version_id BIGINT, attachment_business_type VARCHAR(64), permission_tag JSON, permission_rule JSON, file_name VARCHAR(255) NOT NULL, file_url VARCHAR(512) NOT NULL, storage_object_id VARCHAR(255) NOT NULL, file_hash VARCHAR(128) NOT NULL, file_size BIGINT, mime_type VARCHAR(128), is_main_file TINYINT NOT NULL DEFAULT 0, preview_count BIGINT NOT NULL DEFAULT 0, download_count BIGINT NOT NULL DEFAULT 0, remark VARCHAR(255), create_by BIGINT NOT NULL, create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, update_by BIGINT, update_time DATETIME NULL ON UPDATE CURRENT_TIMESTAMP, deleted TINYINT NOT NULL DEFAULT 0, KEY idx_contract_id(contract_id), KEY idx_attach_type(attach_type), KEY idx_change_id(change_id), KEY idx_storage_object_id(storage_object_id), KEY idx_content_version_id(content_version_id));
CREATE TABLE IF NOT EXISTS t_contract_template (id BIGINT PRIMARY KEY, template_no VARCHAR(64) NOT NULL, template_name VARCHAR(128) NOT NULL, template_category_id BIGINT, template_category_dict_code VARCHAR(64), business_scene VARCHAR(255), scene_tag TEXT, content_format TINYINT NOT NULL DEFAULT 1, is_enable TINYINT NOT NULL DEFAULT 1, sort_num INT NOT NULL DEFAULT 0, remark VARCHAR(500), create_by BIGINT NOT NULL, create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, update_by BIGINT, update_time DATETIME NULL ON UPDATE CURRENT_TIMESTAMP, deleted TINYINT NOT NULL DEFAULT 0, UNIQUE KEY uk_template_no(template_no), KEY idx_template_category_id(template_category_id));
CREATE TABLE IF NOT EXISTS t_contract_template_version (id BIGINT PRIMARY KEY, template_id BIGINT NOT NULL, version_no VARCHAR(16) NOT NULL, content_format TINYINT NOT NULL DEFAULT 1, content MEDIUMTEXT, template_file_id BIGINT, status TINYINT NOT NULL, published_time DATETIME, change_summary VARCHAR(500), create_by BIGINT NOT NULL, create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, update_by BIGINT, update_time DATETIME NULL ON UPDATE CURRENT_TIMESTAMP, deleted TINYINT NOT NULL DEFAULT 0, KEY idx_template_id(template_id));
CREATE TABLE IF NOT EXISTS t_contract_template_type (id BIGINT PRIMARY KEY, template_id BIGINT NOT NULL, category_id BIGINT NOT NULL, category_version_id BIGINT NOT NULL, applicable_scene VARCHAR(255), create_by BIGINT NOT NULL, create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, update_by BIGINT, update_time DATETIME NULL ON UPDATE CURRENT_TIMESTAMP, deleted TINYINT NOT NULL DEFAULT 0, UNIQUE KEY uk_template_cat(template_id,category_id,deleted));
CREATE TABLE IF NOT EXISTS t_contract_template_variable (id BIGINT PRIMARY KEY, template_version_id BIGINT NOT NULL, field_dict_id BIGINT NOT NULL, variable_name VARCHAR(128) NOT NULL, data_source_type TINYINT NOT NULL, format_rule TEXT, is_required TINYINT NOT NULL DEFAULT 0, sort_num INT NOT NULL DEFAULT 0, create_by BIGINT NOT NULL, create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, update_by BIGINT, update_time DATETIME NULL ON UPDATE CURRENT_TIMESTAMP, deleted TINYINT NOT NULL DEFAULT 0, KEY idx_template_version_id(template_version_id));
CREATE TABLE IF NOT EXISTS t_contract_party_snapshot (id BIGINT PRIMARY KEY, contract_id BIGINT NOT NULL, party_ref_id BIGINT NOT NULL, snapshot_json MEDIUMTEXT NOT NULL, snapshot_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, snapshot_version INT NOT NULL DEFAULT 1, snapshot_schema_version VARCHAR(16) NOT NULL DEFAULT '1.0', snapshot_hash VARCHAR(128) NOT NULL, create_by BIGINT NOT NULL, create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, deleted TINYINT NOT NULL DEFAULT 0, KEY idx_contract_id(contract_id), KEY idx_party_ref_id(party_ref_id));
CREATE TABLE IF NOT EXISTS t_contract_change (id BIGINT PRIMARY KEY, contract_id BIGINT NOT NULL, change_no VARCHAR(64) NOT NULL, change_type TINYINT NOT NULL, change_title VARCHAR(255) NOT NULL, change_reason VARCHAR(500), before_content_version_id BIGINT NOT NULL, after_content_version_id BIGINT NOT NULL, effective_content_version_id BIGINT, sign_date DATE, effect_date DATE, approval_status TINYINT NOT NULL DEFAULT 0, approval_no VARCHAR(64), remark VARCHAR(500), create_by BIGINT NOT NULL, create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, update_by BIGINT, update_time DATETIME NULL ON UPDATE CURRENT_TIMESTAMP, deleted TINYINT NOT NULL DEFAULT 0, UNIQUE KEY uk_change_no(change_no), KEY idx_contract_id(contract_id));
CREATE TABLE IF NOT EXISTS t_contract_operate_log (id BIGINT PRIMARY KEY, contract_id BIGINT, target_type VARCHAR(64) NOT NULL, target_id BIGINT, operate_type VARCHAR(64) NOT NULL, operate_desc VARCHAR(500), operate_by BIGINT NOT NULL, operate_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, request_id VARCHAR(128), client_ip VARCHAR(64), before_data MEDIUMTEXT, after_data MEDIUMTEXT, KEY idx_target_type(target_type), KEY idx_target_id(target_id), KEY idx_contract_id(contract_id), KEY idx_operate_by(operate_by), KEY idx_operate_time(operate_time), KEY idx_request_id(request_id));
CREATE TABLE IF NOT EXISTS t_contract_ai_config (id BIGINT PRIMARY KEY, expert_type VARCHAR(64) NOT NULL, provider VARCHAR(64) NOT NULL, base_url VARCHAR(512), app_id VARCHAR(128), app_secret VARCHAR(512), timeout_seconds INT NOT NULL DEFAULT 30, retry_times INT NOT NULL DEFAULT 0, callback_url VARCHAR(512), is_enable TINYINT NOT NULL DEFAULT 1, create_by BIGINT NOT NULL, create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, update_by BIGINT, update_time DATETIME NULL ON UPDATE CURRENT_TIMESTAMP, deleted TINYINT NOT NULL DEFAULT 0, UNIQUE KEY uk_expert_type(expert_type));
CREATE TABLE IF NOT EXISTS t_contract_approval_record (id BIGINT PRIMARY KEY, contract_id BIGINT NOT NULL, change_id BIGINT, approval_no VARCHAR(64) NOT NULL, pre_approval_no VARCHAR(64), contract_content_version_id BIGINT, approval_generation INT NOT NULL DEFAULT 1, approval_action TINYINT NOT NULL, approve_by BIGINT, approve_opinion TEXT, callback_event_id VARCHAR(128), record_status TINYINT NOT NULL DEFAULT 1, operate_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, create_by BIGINT NOT NULL, create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, update_by BIGINT, update_time DATETIME NULL ON UPDATE CURRENT_TIMESTAMP, deleted TINYINT NOT NULL DEFAULT 0, UNIQUE KEY uk_contract_change_gen_no(contract_id,change_id,approval_generation,approval_no), KEY idx_contract_id(contract_id), KEY idx_change_id(change_id));
CREATE TABLE IF NOT EXISTS t_contract_category (id BIGINT PRIMARY KEY, parent_id BIGINT, category_code VARCHAR(64) NOT NULL, category_name VARCHAR(128) NOT NULL, category_level TINYINT NOT NULL, is_enable TINYINT NOT NULL DEFAULT 1, description VARCHAR(500), create_by BIGINT NOT NULL, create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, update_by BIGINT, update_time DATETIME NULL ON UPDATE CURRENT_TIMESTAMP, deleted TINYINT NOT NULL DEFAULT 0, UNIQUE KEY uk_category_code(category_code), KEY idx_parent_id(parent_id), KEY idx_category_level(category_level));
CREATE TABLE IF NOT EXISTS t_contract_category_version (id BIGINT PRIMARY KEY, category_id BIGINT NOT NULL, version_no INT NOT NULL, version_status TINYINT NOT NULL DEFAULT 0, change_summary VARCHAR(500), published_time DATETIME, create_by BIGINT NOT NULL, create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, update_by BIGINT, update_time DATETIME NULL ON UPDATE CURRENT_TIMESTAMP, deleted TINYINT NOT NULL DEFAULT 0, KEY idx_category_id(category_id));
CREATE TABLE IF NOT EXISTS t_contract_field_dict (id BIGINT PRIMARY KEY, field_code VARCHAR(64) NOT NULL, field_name VARCHAR(128) NOT NULL, field_type TINYINT NOT NULL, data_source_type TINYINT NOT NULL, data_source_config MEDIUMTEXT, is_system TINYINT NOT NULL DEFAULT 0, is_enable TINYINT NOT NULL DEFAULT 1, remark VARCHAR(500), create_by BIGINT NOT NULL, create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, update_by BIGINT, update_time DATETIME NULL ON UPDATE CURRENT_TIMESTAMP, deleted TINYINT NOT NULL DEFAULT 0, UNIQUE KEY uk_field_code(field_code));
CREATE TABLE IF NOT EXISTS t_contract_ext_meta (id BIGINT PRIMARY KEY, category_version_id BIGINT NOT NULL, field_dict_id BIGINT, field_code VARCHAR(64) NOT NULL, field_name VARCHAR(128) NOT NULL, field_type TINYINT NOT NULL, is_required TINYINT NOT NULL DEFAULT 0, sort_num INT NOT NULL DEFAULT 0, validate_rule MEDIUMTEXT, create_by BIGINT NOT NULL, create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, update_by BIGINT, update_time DATETIME NULL ON UPDATE CURRENT_TIMESTAMP, deleted TINYINT NOT NULL DEFAULT 0, KEY idx_category_version_id(category_version_id), KEY idx_field_code(field_code));
CREATE TABLE IF NOT EXISTS t_contract_ext_value (id BIGINT PRIMARY KEY, contract_id BIGINT NOT NULL, ext_meta_id BIGINT NOT NULL, field_code VARCHAR(64) NOT NULL, field_type TINYINT NOT NULL, field_value_string VARCHAR(1024), field_value_number DECIMAL(18,4), field_value_decimal DECIMAL(18,2), field_value_date DATE, field_value_json MEDIUMTEXT, content_version_id BIGINT NOT NULL, create_by BIGINT NOT NULL, create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, update_by BIGINT, update_time DATETIME NULL ON UPDATE CURRENT_TIMESTAMP, deleted TINYINT NOT NULL DEFAULT 0, KEY idx_contract_id(contract_id), KEY idx_ext_meta_id(ext_meta_id), KEY idx_content_version_id(content_version_id));
CREATE TABLE IF NOT EXISTS t_contract_template_favorite (id BIGINT PRIMARY KEY, user_id BIGINT NOT NULL, template_id BIGINT NOT NULL, create_by BIGINT NOT NULL, create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, update_by BIGINT, update_time DATETIME NULL ON UPDATE CURRENT_TIMESTAMP, deleted TINYINT NOT NULL DEFAULT 0, UNIQUE KEY uk_user_template(user_id,template_id,deleted));
CREATE TABLE IF NOT EXISTS t_contract_export_log (id BIGINT PRIMARY KEY, export_type VARCHAR(64) NOT NULL, filter_condition MEDIUMTEXT, export_fields TEXT, export_scope_type TINYINT NOT NULL DEFAULT 1, auth_check_result JSON, file_storage_id VARCHAR(255), file_url VARCHAR(512), task_status TINYINT NOT NULL, error_msg VARCHAR(500), operate_by BIGINT NOT NULL, operate_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, create_by BIGINT NOT NULL, create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, update_by BIGINT, update_time DATETIME NULL ON UPDATE CURRENT_TIMESTAMP, deleted TINYINT NOT NULL DEFAULT 0, KEY idx_operate_by(operate_by), KEY idx_operate_time(operate_time));
CREATE TABLE IF NOT EXISTS t_contract_type_permission (id BIGINT PRIMARY KEY, role_id BIGINT NOT NULL, category_id BIGINT NOT NULL, perm_create TINYINT NOT NULL DEFAULT 0, perm_view TINYINT NOT NULL DEFAULT 1, perm_handle TINYINT NOT NULL DEFAULT 0, create_by BIGINT NOT NULL, create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, update_by BIGINT, update_time DATETIME NULL ON UPDATE CURRENT_TIMESTAMP, deleted TINYINT NOT NULL DEFAULT 0, UNIQUE KEY uk_role_cat(role_id,category_id,deleted));
CREATE TABLE IF NOT EXISTS t_contract_template_permission (id BIGINT PRIMARY KEY, role_id BIGINT NOT NULL, template_id BIGINT NOT NULL, perm_use TINYINT NOT NULL DEFAULT 1, create_by BIGINT NOT NULL, create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, update_by BIGINT, update_time DATETIME NULL ON UPDATE CURRENT_TIMESTAMP, deleted TINYINT NOT NULL DEFAULT 0, UNIQUE KEY uk_role_template(role_id,template_id,deleted));
CREATE TABLE IF NOT EXISTS t_contract_field_permission (id BIGINT PRIMARY KEY, role_id BIGINT NOT NULL, field_code VARCHAR(64) NOT NULL, perm_view TINYINT NOT NULL DEFAULT 1, perm_edit TINYINT NOT NULL DEFAULT 0, is_mask TINYINT NOT NULL DEFAULT 0, create_by BIGINT NOT NULL, create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, update_by BIGINT, update_time DATETIME NULL ON UPDATE CURRENT_TIMESTAMP, deleted TINYINT NOT NULL DEFAULT 0, UNIQUE KEY uk_role_field(role_id,field_code,deleted));
CREATE TABLE IF NOT EXISTS t_contract_object_auth (id BIGINT PRIMARY KEY, contract_id BIGINT NOT NULL, auth_target_type TINYINT NOT NULL, auth_target_id BIGINT NOT NULL, perm_view TINYINT NOT NULL DEFAULT 1, perm_edit TINYINT NOT NULL DEFAULT 0, perm_export TINYINT NOT NULL DEFAULT 0, perm_legal_confirm TINYINT NOT NULL DEFAULT 0, auth_status TINYINT NOT NULL DEFAULT 1, auth_start_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, auth_expire_time DATETIME, revoked_by BIGINT, revoked_time DATETIME, auth_remark VARCHAR(500), create_by BIGINT NOT NULL, create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, update_by BIGINT, update_time DATETIME NULL ON UPDATE CURRENT_TIMESTAMP, deleted TINYINT NOT NULL DEFAULT 0, KEY idx_contract_id(contract_id), KEY idx_auth_target(auth_target_type,auth_target_id,deleted), KEY idx_auth_status(auth_status));
CREATE TABLE IF NOT EXISTS t_contract_perform_item (id BIGINT PRIMARY KEY, contract_id BIGINT NOT NULL, content_version_id BIGINT NOT NULL, item_name VARCHAR(255) NOT NULL, item_description TEXT, item_type TINYINT NOT NULL, responsible_user_id BIGINT, evidence_attachment_id BIGINT, plan_finish_date DATE, actual_finish_date DATE, perform_status TINYINT NOT NULL DEFAULT 0, remark VARCHAR(500), sort_num INT NOT NULL DEFAULT 0, create_by BIGINT NOT NULL, create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, update_by BIGINT, update_time DATETIME NULL ON UPDATE CURRENT_TIMESTAMP, deleted TINYINT NOT NULL DEFAULT 0, KEY idx_contract_id(contract_id), KEY idx_content_version_id(content_version_id), KEY idx_responsible_user_id(responsible_user_id));
CREATE TABLE IF NOT EXISTS t_contract_our_subject (id BIGINT PRIMARY KEY, external_source_type TINYINT NOT NULL, external_subject_id VARCHAR(128) NOT NULL, subject_name VARCHAR(255) NOT NULL, is_default TINYINT NOT NULL DEFAULT 0, is_enable TINYINT NOT NULL DEFAULT 1, remark VARCHAR(500), create_by BIGINT NOT NULL, create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, update_by BIGINT, update_time DATETIME NULL ON UPDATE CURRENT_TIMESTAMP, deleted TINYINT NOT NULL DEFAULT 0, UNIQUE KEY uk_ext_sub(external_source_type,external_subject_id,deleted));
CREATE TABLE IF NOT EXISTS sys_dict_type (id BIGINT PRIMARY KEY, dict_name VARCHAR(128) NOT NULL, dict_type VARCHAR(64) NOT NULL, is_active TINYINT NOT NULL DEFAULT 1, remark VARCHAR(256), create_by BIGINT NOT NULL, create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, update_by BIGINT, update_time DATETIME NULL ON UPDATE CURRENT_TIMESTAMP, deleted TINYINT NOT NULL DEFAULT 0, UNIQUE KEY uk_dict_type(dict_type));
CREATE TABLE IF NOT EXISTS sys_dict_data (id BIGINT PRIMARY KEY, dict_type VARCHAR(64) NOT NULL, dict_label VARCHAR(128) NOT NULL, dict_value VARCHAR(128) NOT NULL, sort_num INT NOT NULL DEFAULT 0, is_active TINYINT NOT NULL DEFAULT 1, css_class VARCHAR(64), list_class VARCHAR(64), is_default TINYINT NOT NULL DEFAULT 0, remark VARCHAR(256), create_by BIGINT NOT NULL, create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, update_by BIGINT, update_time DATETIME NULL ON UPDATE CURRENT_TIMESTAMP, deleted TINYINT NOT NULL DEFAULT 0, KEY idx_dict_type(dict_type));
CREATE TABLE IF NOT EXISTS sys_role (role_id BIGINT PRIMARY KEY, role_name VARCHAR(64) NOT NULL, role_key VARCHAR(64) NOT NULL, sort_num INT NOT NULL DEFAULT 0, data_scope TINYINT NOT NULL DEFAULT 1, status TINYINT NOT NULL DEFAULT 1, remark VARCHAR(500), create_by BIGINT NOT NULL, create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, update_by BIGINT, update_time DATETIME NULL ON UPDATE CURRENT_TIMESTAMP, deleted TINYINT NOT NULL DEFAULT 0, UNIQUE KEY uk_role_key(role_key));
CREATE TABLE IF NOT EXISTS sys_user_role (id BIGINT PRIMARY KEY, user_id BIGINT NOT NULL, role_id BIGINT NOT NULL, create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, UNIQUE KEY uk_user_role(user_id,role_id));
CREATE TABLE IF NOT EXISTS sys_role_org (id BIGINT PRIMARY KEY, role_id BIGINT NOT NULL, org_id BIGINT NOT NULL, create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, KEY idx_role_id(role_id));
CREATE TABLE IF NOT EXISTS sys_approval_instance (id BIGINT PRIMARY KEY, approval_no VARCHAR(64) NOT NULL, process_definition_key VARCHAR(64) NOT NULL, process_instance_id VARCHAR(128), biz_type VARCHAR(64) NOT NULL, biz_id BIGINT NOT NULL, title VARCHAR(256) NOT NULL, applicant_id BIGINT NOT NULL, applicant_name VARCHAR(64) NOT NULL, apply_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, current_node VARCHAR(64), current_node_name VARCHAR(64), current_assignee VARCHAR(256), status TINYINT NOT NULL DEFAULT 0, result TINYINT, finish_time DATETIME, total_duration BIGINT, form_data JSON, remark VARCHAR(512), create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, update_time DATETIME NULL ON UPDATE CURRENT_TIMESTAMP, UNIQUE KEY uk_approval_no(approval_no), KEY idx_biz_type_biz_id(biz_type,biz_id), KEY idx_status(status));
CREATE TABLE IF NOT EXISTS sys_approval_node (id BIGINT PRIMARY KEY, instance_id BIGINT NOT NULL, node_key VARCHAR(64) NOT NULL, node_name VARCHAR(64) NOT NULL, node_type TINYINT NOT NULL, assignee_type TINYINT, assignee_id BIGINT, assignee_name VARCHAR(64), candidate_ids VARCHAR(1024), status TINYINT NOT NULL DEFAULT 0, action TINYINT, comment TEXT, attachments JSON, receive_time DATETIME, handle_time DATETIME, duration BIGINT, sort_num INT NOT NULL DEFAULT 0, remark VARCHAR(256), create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, update_time DATETIME NULL ON UPDATE CURRENT_TIMESTAMP, KEY idx_instance_id(instance_id));
CREATE TABLE IF NOT EXISTS sys_user (user_id BIGINT PRIMARY KEY, username VARCHAR(64) NOT NULL, password VARCHAR(128) NOT NULL, emp_id BIGINT, real_name VARCHAR(64) NOT NULL, avatar VARCHAR(256), email_1 VARCHAR(128), phone VARCHAR(32), gender TINYINT, user_type TINYINT NOT NULL DEFAULT 1, status_1 TINYINT NOT NULL DEFAULT 1, last_login_ip VARCHAR(64), last_login_time DATETIME, pwd_update_time DATETIME, remark VARCHAR(500), create_by BIGINT NOT NULL, create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, update_by BIGINT, update_time DATETIME NULL ON UPDATE CURRENT_TIMESTAMP, deleted TINYINT NOT NULL DEFAULT 0, UNIQUE KEY uk_username(username), KEY idx_emp_id(emp_id));
