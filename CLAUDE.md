# CLAUDE.md — 合同管理系统 · 产品说明书

> 本文件定义「我怎么在这个项目里干活」：该做什么、不该做什么、规范是什么。

## 1. 项目是什么
- **通用合同管理系统**（内部和对外各类合同），隶属 HR 产品线。
- 核心卖点：**AI 合同解析** + **AI 法律风险审查**（调 AI 中台「法律专家」）。
- 当前阶段：**一期开发准备与工程骨架初始化**；业务需求和数据库以现行权威文档为准。

## 2. 我该怎么做（工作方式）
1. **只做需求设计 + 数据库表/字段设计，不写业务代码**。
2. 设计要**原创**：只参考「行业通用功能」（公共领域），**不抄任何产品的独特表达**（UI 视觉 / 文案 / 品牌 / 代码）。这是硬红线，防侵权。
3. **按导航栏一个栏目一个栏目梳理**，不跳步、不急着全量铺开。
4. **分清「功能」和「数据」**：功能 = 动词（发起 / 查看 / 提交…）→ 思维导图 / 栏目设计；数据 = 名词 / 字段 → 数据库表设计。别把字段当功能块。
5. 用户要求「完整设计 + 分期标签（一期/二期/三期）」，**每项功能都要标期次，不能只设计一期**。

## 3. 数据库设计规范（长期有效）
- 表前缀：`t_` 业务表，`sys_` 系统表。
- 主键：`id` BIGINT **雪花非自增**。
- 逻辑删除：`deleted`（软删除）。
- 公共审计字段：`create_by / create_time / update_by / update_time / deleted`。
- 枚举字段一律 `TINYINT`（禁止字符串存枚举）。
- 字段后缀：`_id/_time/_date/_type/_status/_num/_url/_no/_desc`，布尔用 `is_` 前缀。
- 索引：`PRIMARY KEY`、`UNIQUE KEY uk_*`、`KEY idx_*`。
- **遵循《人力资源管理系统数据库设计文档4》标准**（id 雪花 + deleted）；公司另有「培训模块标准」（id 自增 + is_deleted），本项目**不用**。

## 4. 不要做的事
- 不要写业务代码。
- 不要把「每个名词」都当成一个模块。
- 不要把「数据字段」当成「功能」画进思维导图。
- 不要抄袭任何产品的独特表达。
- 不要跳步骤（按栏目一个一个来）。
- 不要在用户没确认时，擅自把结论当定稿写进单一事实来源。

## 5. 关键文件
- **需求设计（当前详细基线）**：`合同管理系统需求设计.md`
- **需求思维导图（当前评审主入口）**：`合同管理系统需求思维导图.md`
- **需求思维导图 XMind 导入版**：`合同管理系统需求思维导图-XMind导入.txt`（Tab 缩进，XMind 直接导入分层）
- **数据库表设计（字段级唯一权威版）**：`D:\project\hr-contract\数据库表\合同管理.docx`（用户持续维护的最新版本；不得根据其他 Markdown 推断表数量或字段）
- **AI 专家体需求设计**：`AI专家体需求设计.md`
- **元文件**：`CLAUDE.md`（本文件）/ `Memory.md`（进度）/ `Learning.md`（复盘）/ `Wiki.md`（常识库）
- **开发实施唯一入口**：`开发实施规划.md`（技术、接口、页面切片、Mock、测试和开发路线）
- **归档目录**：`归档/`（历史/过时文件，如旧评审稿、旧数据库表 md）

## 6. 维护规则（重要）
- **每 3 轮对话做一次「注入优化」**：把这 3 轮的新决策、新进度分别注入 Memory / Wiki / Learning。
- **CLAUDE.md 控制在 300 行以内**。
- 重要结论**不能省略**，必须完整落到对应文件里。
- 开发按业务批次推进：同一闭环内的相关能力（例如正文版本、文件附件和导入）一次性设计、实现、测试和交付，不拆成每轮一个小确认点。
- 每个批次开始前只说明目标、原因、改动范围、验证方式和暂不处理内容；完成后直接进入下一批。只有产品范围、权威数据库结构或外部不可逆操作需要用户决策时才暂停确认。
