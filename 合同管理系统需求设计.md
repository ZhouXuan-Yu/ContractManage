# 通用合同管理系统需求设计

> 状态：当前需求基线，供需求评审和数据库设计使用
>
> 说明：本稿依据《人力资源管理系统数据库设计文档4》及已确认的新合同方案重新整理；字段级建表设计以 `D:\project\hr-contract\数据库表\合同管理.docx` 为唯一权威来源。本需求文档不维护表数量和完整字段清单。

## 1. 项目定位

建设一个面向企业的通用合同全生命周期管理系统。

系统只围绕“合同”提供能力，统一管理企业内部和对外签订的各类合同。员工、客户、供应商、咨询机构等只作为合同交易对方或关联主体出现，不在本系统重复建设其主数据管理能力。

系统不自建审批引擎。审批流程由外部统一审批系统负责，本系统通过接口发起、查询、接收结果并同步合同状态。

## 2. 系统边界

### 2.1 本系统负责

- 合同类型和合同模板管理
- 合同新建、导入、编辑和版本管理
- 合同正文、原始文件和附件管理
- 合同交易对方引用和关键时点快照
- 合同通用信息和类型扩展信息
- 合同状态和履行信息
- 收付款计划及履行记录
- 合同变更和补充协议
- AI 合同解析和法律风险审查
- 外部审批系统集成
- 合同操作审计和查询统计

### 2.2 外部系统负责

- HR 系统：员工、组织、岗位等员工主数据
- 客户/供应商等主数据系统：客户、供应商及其他外部主体主数据
- 统一审批系统：流程、节点、审批人、审批页面和审批流转
- 文件服务：文件上传、下载、预览和权限校验
- AI 平台：AI 工作流、提示词、模型编排和结构化结果生成；一期默认可采用 Dify 私有化

本系统只保存外部对象的关联 ID、必要展示信息和合同关键时点快照，不替代外部系统成为主数据源。

### 2.3 文件服务与合同中心区

公司已有统一文件服务，“合同中心区”是该文件服务为合同系统分配的业务存储空间，用于保存 PDF、Word、扫描件、签署文件、履行佐证、变更补充协议附件和导出文件。它属于文件服务底座，不属于合同系统业务代码，也不是合同系统菜单。

```text
合同系统（合同权限、附件关联、版本、审计）
    -> 公司文件服务（上传、物理存储、预览、下载、临时访问链接）
        -> 合同中心区（实际文件）
```

合同系统不得把文件二进制、物理绝对路径或长期公开 URL 存入业务数据库；`t_contract_attachment` 只保存文件服务返回的 `file_id`、对象标识、文件名、格式、大小、哈希、附件业务分类、关联正文版本/变更记录和上传审计信息。

文件服务建议按业务对象隔离对象键，具体命名由文件服务统一约定，例如：

```text
contract-center/contracts/{contract_id}/original/
contract-center/contracts/{contract_id}/signed/
contract-center/contracts/{contract_id}/attachments/
contract-center/contracts/{contract_id}/changes/{change_id}/
contract-center/templates/{template_id}/
contract-center/exports/{export_log_id}/
```

目录只是文件服务内部对象键约定，不允许合同前端直接拼接或访问。

## 3. 合同类型范围

合同系统不限制为商务合同，建议采用以下合同分类目录：

- 用工与人员类：劳动合同、劳务协议、顾问协议
- 交易合作类：采购合同、销售合同、服务合同、咨询合同
- 资产与资金类：租赁合同、借款合同、担保协议
- 权利与成果类：保密协议、知识产权协议、许可协议
- 其他组织自定义合同

补充协议和变更协议不是合同类型，而是依附于原合同的变更关系和业务能力，必须关联原合同，不作为独立类型列入目录。

劳动合同专指公司与员工之间的劳动关系合同；劳务协议和顾问协议虽然可以由自然人作为对方，但不自动等同于劳动合同，应作为独立合同类型，使用不同的模板、字段和 AI 审查规则。

以上目录是推荐的产品分类，不代表所有类型都必须一次性开发完整；类型可配置启用和分期交付。

## 4. 合同类型模型

### 4.1 合同类型

合同类型采用“合同大类 + 合同类型 + 合同子类型”的层级模型，并用独立维度记录交易方向、协议性质和我方角色，不把多个维度混成一个平铺枚举。

推荐分类层级：

- 用工与人员类：劳动合同、劳务协议、顾问协议
- 交易合作类：采购、销售、服务、咨询
- 资产与资金类：租赁、借款
- 权利义务类：保密、知识产权、许可
- 其他自定义类

采购、销售、服务属于合同类型或业务场景；保密、知识产权属于协议性质标签。一个合同可以有一个主合同类型，同时拥有多个性质标签、一个交易方向和一个我方角色（甲方/乙方等）。

每个合同大类或合同类型至少配置：

- 类型名称和编码
- 上级类型
- 是否启用
- 可用模板
- 类型扩展字段
- 适用的交易对方类型
- 适用的 AI 解析和审查规则
- 可选的合同履行信息

### 4.2 合同子类型

合同子类型用于表达同一大类下的进一步差异，例如：

- 固定期限劳动合同
- 无固定期限劳动合同
- 房屋租赁合同
- 设备租赁合同

业务场景可用于表达采购、销售、服务等流程和字段差异。子类型不是必须单独建业务模块，应优先通过类型配置、模板和扩展字段表达。

### 4.3 合同模板

合同模板是可以实际起草合同正文的文件或文本模板。一个合同类型可以有多个模板，一个模板可以有多个版本。

合同类型、合同子类型和合同模板必须分开设计，不能把模板名称直接当成合同类型。

模板数量较多时，模板库必须支持分层组织和快速定位：

- 模板分类：按标准版、框架版、加急版等模板用途分组。
- 适用场景：说明模板适用于采购、续签、特定业务线或特定组织场景。
- 标签：支持多个业务标签，便于组合筛选。
- 收藏和常用：用户可以收藏模板，系统可以记录常用模板，工作台可直接从常用模板发起合同。
- 检索：按模板名称、关键词、分类、合同类型、适用场景、标签和启用状态查询。
- 批量导入：支持 Word 模板文件和模板元数据批量导入；Excel 用于模板元数据、变量映射和批量配置，不默认作为合同正文模板。
- 模板发布：草稿、待发布、已发布、已停用等版本状态独立管理；发布前校验变量来源和必填配置。

模板变量统一引用系统字段字典，不为每个模板重复定义同一个字段。字段字典同时作为模板变量、合同类型扩展字段和 AI 解析输出字段的标准编码来源。

合同字段和模板内容分三层：

1. 通用合同字段：所有合同稳定共有，例如编号、名称、我方主体、我方角色、交易对方、金额、日期、正文、附件、状态和版本。
2. 类型扩展字段：由合同类型和子类型决定是否显示和是否必填，例如劳动合同的试用期、采购合同的验收标准、租赁合同的押金。
3. 模板变量和正文：具体模板从字段字典中选择可用字段，负责变量排版、条款组合和适用场景，不自行创建业务字段。

因此不同合同类型可以有不同字段、不同模板和不同 AI 规则，但仍使用同一套合同主流程和通用数据底座。模板差异主要体现在正文条款、变量组合、排版和适用场景；业务字段差异由合同类型配置管理。

### 4.4 模板生成合同

模板生成是合同系统的核心能力，不由 AI 自由生成最终合同正文。系统根据合同类型、子类型和用户选择的模板，读取统一字段字典中的通用字段和类型扩展字段，填充模板变量并生成合同草稿；用户确认、编辑后保存为合同正文版本。

```text
选择合同类型和模板
  -> 加载通用字段和类型扩展字段
  -> 填充模板变量
  -> 校验必填变量和未填充变量
  -> 生成合同草稿
  -> 人工编辑确认并保存正文版本
```

模板引擎负责变量替换、条款组合、格式渲染和正文版本生成。AI 不直接替代模板引擎生成最终合同，但可以对生成结果进行字段完整性校验、合同解析和法律风险审查。

## 5. 通用合同底座

所有合同共用以下业务概念：

- 合同编号和名称
- 合同类型和子类型
- 合同来源：模板起草、文件导入、手工录入、外部系统同步
- 合同状态
- 我方主体和我方角色
- 交易对方引用
- 金额、币种和收付方向（非金额型合同允许为空）
- 签订日期、生效日期、到期日期
- 合同正文和原始文件
- 合同附件
- 内容版本和内容摘要
- 履行状态
- 变更和补充协议关系
- AI 解析与审查任务
- 外部审批关联
- 创建人、更新时间和操作日志

通用字段只表达所有合同都稳定共有的概念，不能因为某一种合同需要就把类型专属字段塞进主合同表。

## 6. 类型扩展

类型特有内容通过“合同类型字段配置 + 合同类型字段值”表达，必要时再为高频、强查询、强约束的业务建立专用明细结构。

### 6.1 示例

员工合同：

- 合同期限类型
- 试用期
- 合同岗位
- 合同薪酬
- 工作地点

采购合同：

- 采购物或服务项
- 交付日期
- 验收标准
- 质保期限
- 供应商付款条件

租赁合同：

- 租赁物
- 租赁开始和结束日期
- 租金
- 租金递增规则
- 押金

借款合同：

- 借款本金
- 利率
- 还款方式
- 还款计划
- 担保方式

### 6.2 扩展字段规则

- 字段名称、编码、类型、是否必填、是否可见、是否可编辑应可配置
- 类型字段由合同类型控制，不对所有合同显示
- 金额、日期、数字等需要使用结构化类型，不能全部保存为文本
- 复杂、低频或配置型内容可使用 JSON
- 会参与筛选、统计、排序或强校验的字段，后续评估是否独立结构化
- 类型扩展字段也必须支持版本和历史追溯

### 6.3 统一字段字典

模板变量、合同通用字段、合同类型扩展字段和 AI 解析结果必须使用统一字段编码。例如合同编号、我方主体、交易对方、合同金额、签署日期、工作地点、交付日期和付款条件，都只能在字段字典中定义一次，再由类型、模板和 AI 任务引用。

字段字典至少包含：字段编码、字段名称、数据类型、格式化规则、数据来源、是否可为空、是否敏感、适用合同类型、原文提取规则和展示规则。字段字典发布新版本后，历史合同和历史 AI 结果仍绑定原字段版本。

## 7. 交易对方引用

交易对方只是合同的关联信息，不是本系统独立管理的业务主体。本系统不提供主体主数据的新建、编辑、启停用、删除或主体统计模块。

### 7.1 三个区分维度

- 主体来源：HR/组织系统或外部交易对方主数据系统；后者可能是一套系统，也可能是多套系统。
- 合同中的主体身份：员工、客户、供应商、咨询机构、合作方、其他组织或个人。
- 主体自然属性：企业、个人或其他组织。

“企业/个人”只是主体自然属性，不能替代“员工/客户/供应商”等合同业务身份；同一主体在不同合同中可以拥有不同合同身份。

### 7.2 关联规则

合同创建时从外部主体系统查询并选择主体，合同侧至少保存：

- 来源系统
- 外部主体 ID
- 合同中的主体身份
- 外部系统返回的当前展示信息
- 外部主体版本或更新时间
- 提交、签署或导出时形成的必要快照

员工合同统一进入合同系统，员工主数据仍由 HR 系统维护。员工 ID 只作为 HR 来源系统的外部主体 ID 保存，不再作为类型扩展字段重复保存。

外部主体名称、组织、岗位或状态发生变化时，不得修改已固化的历史合同快照。外部主体停用后不能用于新合同，但历史合同继续可查。

### 7.3 本系统不做的主体功能

- 不建设主体列表卡片和主体独立导航。
- 不在本系统录入客户、供应商或员工主档案。
- 不在本系统执行主体主数据去重。
- 不在本系统维护主体启停用和删除。
- 不把合同关联统计扩展成 CRM 或供应商管理。

## 8. 合同生命周期

合同生命周期需要支持不同合同类型配置不同的业务动作，但所有合同共享基本生命周期：

```text
草稿 -> 待签署 -> 履行中 -> 已完成
  |       |\
  |       +----> 草稿（撤回签署，原审批实例作废并生成新实例）
  +------> 作废

履行中 -> 已终止
```

具体是否经过审批，不由合同状态机硬编码决定，而由外部审批集成配置和合同类型规则决定。

合同提交外部审批后，合同业务状态仍保持“草稿”，由 `approval_status` 表示“审批中”。当 `approval_status` 为审批中时，合同正文、交易对方、金额和类型扩展等关键内容锁定；审批驳回或撤回后恢复可编辑。合同列表按审批状态筛选时使用 `approval_status`，不能依赖合同业务状态。

合同系统必须区分：

- 合同业务状态
- 外部审批同步状态
- AI 解析状态
- AI 审查任务状态
- 合同履行状态

签约前允许作废；签约生效后不再作废，使用终止或解除记录。

## 9. 外部审批集成

本系统不实现审批引擎，不维护审批节点和审批人配置。

### 9.1 集成能力

- 创建外部审批实例
- 保存外部审批实例 ID 和审批单号
- 查询外部审批状态
- 撤回外部审批
- 接收审批结果回调
- 处理回调幂等
- 处理回调丢失、超时和重试
- 根据审批结果推动合同状态
- 保存审批同步记录和审计信息

### 9.2 接口适配层

合同业务只依赖本系统内部的审批适配接口，底层可以替换为：

- Mock 审批服务
- 测试审批系统
- 正式统一审批系统

真实审批系统尚未完成时，使用 Mock 完成合同系统开发和自动化测试，不能等待外部系统完成后再开发关联能力。

### 9.3 关键关联信息

至少需要设计：

- approval_provider
- approval_instance_id
- approval_no
- approval_status
- approval_sync_time
- approval_result

审批系统负责流程和审批页面，合同系统负责合同数据、关联关系、状态同步和本地审计。

## 10. AI 合同专家体

AI 专家体继续保留。当前一期建议使用 Dify 私有化作为外部 AI 工作流和模型编排平台，但系统必须通过统一 AI Provider 适配层接入，不能把 Dify 作为永久硬依赖。

### 10.1 两项核心能力

1. 合同解析：从 PDF、DOCX 或合同正文中抽取通用字段、类型字段、条款、标的和收付款计划。
2. 法律风险审查：按合同类型、我方角色和审查视角输出风险等级、风险依据、风险说明和修改建议。

### 10.2 系统与 AI 平台的边界

合同系统负责：

- 保存原始文件和合同正文
- 创建 AI 任务
- 传递合同类型和审查上下文
- 保存 AI 结果
- 任务幂等、重试和失败记录
- 人工确认、修正和版本追踪
- 调用模板引擎生成合同草稿，并保存模板版本和正文版本关联

AI 平台负责：

- 工作流编排
- 合同类型路由
- 提示词和审查规则
- 模型调用
- 结构化 JSON 输出

AI 平台不负责最终合同模板渲染、审批、签署或合同状态变更。模板引擎负责根据模板版本和字段值生成合同草稿，AI 只对生成结果进行解析、校验和风险审查。

合同系统通过统一 AI Provider 适配层接入 AI 平台，至少预留 `mock`、`dify`、直接模型 API 和其他平台实现。任务提交、状态查询、结果获取、取消、重试和连通性测试使用统一内部契约；业务代码不得依赖第三方返回结构。AI 任务状态、超时重试、幂等、合同版本绑定、Schema 校验、结果落库、人工确认、权限和审计均由合同系统负责。

建议先建设一个统一合同 AI 专家体，通过 `contract_type`、`task_type` 和 `review_view` 路由不同合同类型，不为每种合同单独建设一套 AI 系统。

### 10.3 AI 结果要求

- 返回结构必须带 schema_version
- 结果必须关联合同内容版本和任务号
- AI 结果不能直接改变合同状态
- AI 结论不能代替人工法律确认
- 内容版本变化后，旧 AI 结果必须标明对应版本
- 超长合同支持分段处理和结果合并

## 11. 权限和敏感数据

权限配置参考 HR 招聘系统已有的角色菜单权限、角色数据范围和后端数据过滤模型，在合同系统中扩展合同类型、模板、字段和合同级授权。权限以角色、组织/部门、合同负责人、合同类型、操作动作和字段敏感等级共同控制。合同大类和合同类型负责定义业务规则，其中合同类型是实际授权单位；合同大类只负责导航分组，不直接作为用户可见条件。

### 11.1 合同类型与数据可见范围

不同业务人员不需要进入不同的合同系统，也不为销售、人力分别建设合同菜单。所有人进入统一“合同中心”，系统根据权限过滤合同列表、工作台指标、可创建类型、可用模板和详情字段。

默认数据范围建议：

- 系统管理员、合同管理员：按授权查看和管理全部或指定组织合同。
- 法务：查看授权范围内合同，通常可查看法律审查内容和敏感风险信息。
- 销售人员：默认查看本人创建或负责、所属组织范围内的销售和服务类合同；具体范围由组织和角色配置决定。
- HR 人员：默认查看劳动、劳务、顾问等用工与人员类合同；员工合同的薪酬、身份证号等字段另行脱敏授权。
- 采购人员：默认查看采购合同及其履行、验收和付款信息。
- 只读用户：只能查看被授予的数据范围，不能编辑或执行流程动作。

一期默认可见范围采用交集规则。这里的组织数据范围复用 HR 系统已有的基础范围，不表示一期支持用户跨多个部门或多个组织叠加授权。`deleted=0`（未软删除）是数据保留条件，不等于合同业务状态未作废；作废合同仍保留并可按权限查看，作废只影响有效业务展示和统计，不作为权限过滤条件：

```text
默认可见合同
    = HR基础组织数据范围
      ∩ 合同类型权限
      ∩ 未软删除条件（`deleted=0`，不等于未作废）
```

合同大类不是独立权限判断单位。合同类型从属于合同大类，合同类型权限是实际配置和校验单位；某大类下至少有一个启用合同类型对用户可见时，该大类才显示。管理员可以使用“大类批量授权”作为配置快捷方式，但系统实际按当前类型展开保存和校验权限；后续新增合同类型不自动获得权限，需重新授权。作废合同仍属于可见合同范围，是否展示由用户权限决定；是否计入有效统计由合同状态统计口径决定。

一期基础组织数据范围包括 HR 系统已有的本人、本部门、本部门及下级、指定组织等范围。用户同时属于多个部门、多个组织范围叠加，或不同角色产生多个组织范围并集，统一列入二期多部门/多组织数据隔离能力。

例如销售人员即使拥有销售合同类型权限，也只能看到本人或本部门数据范围内的销售合同，不能直接查看其他部门的销售合同。合同级授权是受控例外，只对指定合同、指定用户或角色、指定动作和有效期生效：

```text
最终可见合同
    = 默认可见合同
      ∪ 有效合同级授权
```

合同级授权不能扩大用户的系统管理权限；授权人必须具备授权权限，所有授权和撤销都要记录审计。字段权限独立于合同权限，拥有合同查看权不代表可以查看薪酬、身份证号、借款利率和担保信息。

新建合同页面只展示当前用户有权创建的合同类型、子类型和模板；合同大类入口由可创建类型派生。选择类型后动态加载通用字段、类型扩展字段、可用模板和 AI 规则。工作台卡片、统计结果和台账查询必须使用同一数据权限过滤条件。

### 11.2 权限配置范围

系统管理中的“权限配置”沿用 HR 招聘系统的基础模型，并增加合同业务权限：

- 角色菜单权限：控制合同中心和系统管理的可见性。
- 页面与按钮权限：控制新建、编辑、AI、审批、签署、附件、导出、作废和终止等动作。
- 数据范围权限：支持本人、本部门、本部门及下级、指定组织、全部、合同负责人等范围。
- 合同类型权限：控制角色可创建、查看和处理的合同类型及其子类型；合同大类只作为导航分组，不单独作为运行时权限条件。
- 模板使用权限：控制角色可使用的模板分类、适用场景和具体模板。
- 字段权限：控制字段可见、脱敏、只读和可编辑；敏感字段独立授权。
- 合同级授权：为跨部门协作临时授予指定用户或角色合同操作权限，并配置有效期和授权范围。

权限过滤规则属于“系统管理 → 权限与审计”的配置说明和系统运行规则，不是独立导航。合同中心和履行页面只展示经过权限过滤后的结果。

权限判定顺序为：角色菜单权限决定入口；页面与按钮权限决定动作；一期 HR 基础组织数据范围与合同类型权限取交集，形成默认合同集合；二期再支持多部门、多组织范围叠加；模板权限决定可使用的模板；字段权限决定字段展示和编辑方式；有效合同级授权只补充指定合同的例外范围。后端必须在每次查询和写操作时重新校验权限，前端隐藏按钮不能替代后端鉴权。

权限表建议复用 HR 系统的角色、菜单、用户和组织数据范围表，合同系统新增合同类型权限表、模板权限表、字段权限表和合同级授权表，不复制基础 RBAC。

员工合同、身份证号、薪资、借款利率等敏感信息需要独立权限和脱敏展示。系统管理员、合同管理员、法务、业务人员和只读用户的权限不能完全相同。

合同系统不复制外部主体的完整敏感档案；合同自身产生的敏感字段和签署快照必须按合同权限保护。

## 12. 一期范围建议

一期先实现通用底座和少量高频类型：

- 合同通用台账
- 员工、采购、销售、服务合同基础支持
- 模板起草、文件导入和手工录入
- 正文和附件管理
- 合同类型扩展字段
- AI 解析和法律审查
- 外部审批适配接口和 Mock 服务
- 合同状态同步
- 变更、补充协议和审计日志

租赁、借款、知识产权等类型先完成类型目录、模板和字段配置方案，再根据实际业务量逐步启用。

## 13. 推荐设计结论

本方案先采用以下默认设计，不因每个细节都未在老板原话中出现而暂停整体设计：

- 员工合同统一进入合同系统；劳动合同、劳务协议、顾问协议按不同合同类型管理。
- 合同支持“我方主体”引用，默认单主体部署，但数据模型预留母公司、子公司等多法人扩展能力。
- 合同分类使用大类、类型、子类型三层结构；交易方向和协议性质单独建维度。
- 补充协议和变更协议通过原合同关系管理，不能脱离原合同独立创建。
- 外部主体和外部审批系统均采用接口引用；合同系统通过适配层和 Mock 完成开发测试。
- AI 使用统一合同专家体，通过合同类型、任务类型和审查视角路由，底层通过 AI Provider 接入；一期默认可采用 Dify。

## 14. 一期开发基线定稿

一期启用劳动合同、劳务协议、顾问协议、采购合同、销售合同和服务合同；每类先提供“通用”子类型，后续通过配置扩展。合同来源统一为模板起草、文件导入、手工录入和外部系统同步。

客户、供应商、合作方和员工统一归为交易对方。`party_source_type` 表示来源系统，`party_role` 表示签约角色，`business_relation_type` 表示客户/供应商/合作方/员工等合同业务关系，三者不混用。外部系统正式接口、鉴权和 ID 规则由 Adapter 契约承接，一期使用 Mock，不阻塞开发。

审批由外部审批系统负责，AI 由统一合同 AI 专家体负责；合同系统分别保存审批记录、AI 任务、人工确认结果和审计记录，不让 AI 直接改变合同状态或替代审批。

## 15. 后续设计顺序

1. 以本方案为基线梳理所有合同的通用流程
2. 梳理每种合同类型的专属字段、模板、附件和 AI 规则
3. 确认外部主体、文件和审批系统接口契约
4. 依据《人力资源管理系统数据库设计文档4》重新设计数据库
5. 设计 API、页面、Mock 服务和测试用例
6. 仅对第 14 节事项进行业务确认

## 16. 合同类型差异矩阵

以下矩阵用于区分通用能力和类型能力。矩阵中的“支持”表示系统需要具备配置或扩展能力，不表示所有类型在一期都必须全部上线。

| 合同大类 | 合同类型 | 常见对方 | 关键扩展信息 | 履行重点 | AI 审查重点 |
|---|---|---|---|---|---|
| 用工与人员 | 劳动合同 | 员工 | 合同期限、试用期、岗位、工作地点、薪酬 | 入职、续签、解除、到期 | 劳动关系、期限、薪酬、解除和竞业 |
| 用工与人员 | 劳务协议 | 个人/机构 | 服务内容、服务期限、报酬、成果交付 | 服务交付、结算 | 劳务关系、责任边界、税费和成果 |
| 用工与人员 | 顾问协议 | 顾问/机构 | 顾问范围、工作量、报酬、成果 | 咨询交付、结算 | 服务责任、知识产权、保密 |
| 交易合作 | 采购合同 | 供应商 | 采购标的、数量、交付、验收、质保 | 交付、验收、付款 | 质量、交付、违约、付款 |
| 交易合作 | 销售合同 | 客户 | 销售标的、交付、验收、收款 | 发货、验收、收款 | 收款、交付、责任和违约 |
| 交易合作 | 服务合同 | 客户/合作方 | 服务范围、周期、验收、SLA | 服务履约、验收、收付款 | 服务边界、验收、责任和续期 |
| 交易合作 | 咨询合同 | 咨询机构 | 咨询范围、交付物、里程碑 | 里程碑、成果验收 | 成果归属、责任、保密 |
| 资产与资金 | 租赁合同 | 出租方/承租方 | 租赁物、租期、租金、押金、递增 | 租赁期间、付款、返还 | 租期、费用、维修、违约 |
| 资产与资金 | 借款合同 | 金融机构/组织/个人 | 本金、利率、期限、还款、担保 | 还本付息 | 利率、担保、提前还款、违约 |
| 权利与成果 | 保密协议 | 员工/客户/合作方 | 保密信息、例外、期限、违约 | 保密期限和解除 | 保密范围、期限、例外、违约 |
| 权利与成果 | 知识产权协议 | 员工/合作方/机构 | 权利归属、许可范围、地域、期限 | 权利交付和许可 | 权利归属、许可、侵权责任 |
| 其他 | 自定义合同 | 按业务配置 | 自定义字段、模板和规则 | 按类型配置 | 使用通用审查规则 |

## 17. 通用业务流程

### 17.1 创建合同

合同创建支持四种来源：模板起草、文件导入、手工录入、外部系统同步。

创建时必须选择合同大类和合同类型，系统根据类型加载对应模板、字段、必填项、对方类型和 AI 规则。

### 17.2 编辑和版本

- 草稿状态允许编辑通用字段、类型字段、正文和附件。
- 保存合同正文时递增内容版本并生成内容摘要。
- 关键内容发生变化时，历史 AI 结果标记为对应旧版本，不得覆盖新版本结果。
- 合同提交外部审批后，关键字段和正文锁定。
- 审批驳回或撤回后恢复编辑，并保留原审批关联和历史记录。

### 17.3 AI 解析

1. 上传或保存合同正文。
2. 系统生成解析任务和请求版本。
3. AI Provider 按合同类型抽取通用字段、类型字段、条款、标的和付款计划。
4. 系统保存结构化结果和原文依据。
5. 用户查看差异并确认或修正。
6. 人工确认结果成为当前合同数据，保留 AI 原始结果。

AI 解析失败不阻断合同保存，失败任务允许自动重试和人工重发。

### 17.4 AI 法律审查

1. 用户选择审查视角和合同版本。
2. 系统创建审查任务并提交 AI Provider。
3. AI Provider 按合同类型和审查视角执行规则。
4. 系统保存风险等级、风险项、原文依据、理由和建议。
5. 法务查看、确认或修改意见。
6. 审查结果与合同内容版本绑定，支持多次审查和历史对比。

AI 风险等级只用于提示和筛选，不自动通过、驳回或改变合同状态。

### 17.5 外部审批

1. 合同完成提交前校验。
2. 系统向外部审批系统创建审批实例。
3. 合同保持草稿，`approval_status` 进入审批中并锁定关键内容。
4. 外部系统通过回调或查询返回审批结果。
5. 系统记录结果并更新 `approval_status`。
6. 审批通过后合同进入待签署；驳回或撤回后回到可编辑草稿。

外部审批系统不可用时，合同系统记录失败原因，不得产生半成功的审批关联。

### 17.6 签署和履行

- 待签署状态允许上传已签署文件或接收电子签署结果。
- 待签署状态允许撤回签署，撤回后合同回到草稿；原待签署关联作废，重新提交时生成新的审批实例，历史记录保留。
- 签署生效后记录签署日期和生效日期，必要时固化交易对方快照。
- 履行中支持履行事项、付款计划、验收记录和附件更新。
- 履行完成后进入已完成。
- 履行中提前解除进入已终止，并记录解除原因、时间和依据。

### 17.7 变更和补充协议

- 只能从原合同发起变更或补充协议。
- 变更记录必须关联原合同。
- 补充协议可以有独立正文、附件、版本和外部审批关联。
- 变更生效前不改变原合同有效数据。
- 变更生效后，系统展示当前有效内容和历史版本差异。
- 原合同完成或终止后是否允许补充协议，由合同类型规则配置。

## 18. 功能需求清单

### 18.1 合同台账

- 按编号、名称、类型、对方、状态、审批状态、日期和金额查询。
- 正文关键词检索列为二期能力，需依赖文件全文索引，不作为一期简单模糊查询实现。
- 支持合同大类、合同类型、交易方向、协议性质等筛选。
- 展示合同状态和审批状态两个独立状态。
- 展示 AI 解析和 AI 审查状态。
- 支持新建、导入、查看、编辑、作废、导出和发起 AI 任务。
- 作废合同保留展示并标识状态，不参与有效合同统计。

### 18.2 合同详情

详情至少包括：

- 基本信息
- 交易对方
- 类型扩展信息
- 合同正文
- 条款和标的
- 收付款计划
- 附件
- AI 解析
- AI 法律审查
- 外部审批状态
- 待签署撤回及历史审批关联
- 变更和补充协议
- 履行记录
- 操作日志

### 18.3 合同类型和字段配置

- 管理合同大类、类型、子类型和标签。
- 配置类型字段名称、编码、数据类型、必填、可见、可编辑和校验规则。
- 配置类型适用模板、对方类型、交易方向、协议性质和 AI 规则。
- 类型配置变更只影响新建合同，历史合同保留原类型配置版本。

### 18.4 模板管理

- 管理模板库、模板分类、适用场景、标签、版本、适用类型和变量。
- 支持按名称、关键词、分类、适用类型、适用场景、标签、启用状态和收藏查询。
- 支持 Word 模板导入、模板元数据和变量映射批量导入、单个模板新建和模板复制。
- 支持常用模板和个人收藏模板，并从工作台快捷发起合同。
- 模板变量统一引用字段字典；字段字典同时服务合同类型扩展和 AI 解析结果。
- 支持模板启用、停用、预览、发布和历史版本查看。
- 新建合同时只能选择当前启用模板。
- 模板变更不覆盖已创建合同正文。

### 18.5 交易对方引用

- 调用外部主体系统查询可用主体。
- 按来源系统、合同主体身份和主体自然属性筛选。
- 选择员工、客户、供应商等外部主体关联合同。
- 展示外部系统返回的当前信息，不在本系统维护主档案。
- 外部主体停用后不能用于新合同，但历史合同继续展示。
- 在关键时点保存合同侧快照。

### 18.6 附件和文件

- 支持原始合同、已签署合同、补充协议、证明材料和其他附件。
- 支持附件上传、预览、下载、版本查看和删除；删除附件仅系统管理员或合同管理员可执行，并必须经过文件服务和权限校验。
- 记录文件名称、大小、格式、摘要、来源和关联合同版本。
- 审计日志记录下载和预览等访问行为。

### 18.7 履行与收付款

- 金额型合同支持收付款方向、计划金额、计划日期和付款条件。
- 支持已收付金额、实际日期、状态和逾期标记。
- 非金额型合同不强制产生付款计划。
- 履行事项应支持按合同类型配置。

### 18.8 审计日志

- 记录合同、正文、类型字段、附件、AI 任务和审批同步操作。
- 记录操作人、时间、IP、操作类型和前后数据摘要。
- 敏感数据不得写入日志明文。
- 日志在保留期内不可编辑和删除。

## 19. 角色与权限

| 角色 | 合同 | 类型/模板 | AI 审查 | 审批状态 | 敏感信息 |
|---|---|---|---|---|---|
| 系统管理员 | 管理 | 管理 | 查看 | 查看/同步处理 | 按授权 |
| 合同管理员 | 管理业务合同 | 管理 | 发起/查看 | 查看/跟踪 | 按授权 |
| 法务 | 查看合同 | 无 | 发起/编辑意见/确认 | 查看 | 可查看敏感审查内容 |
| 业务人员 | 创建和维护本人合同 | 使用模板 | 发起/查看 | 查看本人关联状态 | 最小权限 |
| 只读用户 | 按数据范围查看 | 查看 | 仅查看 | 仅查看 | 脱敏 |

系统必须支持按角色、组织和合同数据范围授权。员工合同、薪资、身份证号、借款和担保等字段需要更严格的字段级权限。

## 20. 外部接口需求

### 20.1 外部主体接口

- 按关键词和主体类型查询主体。
- 根据外部主体 ID 获取当前展示信息。
- 校验主体是否启用、是否允许签约。
- 返回主体来源系统和版本信息。

### 20.2 文件服务接口

- 申请上传：合同系统完成业务权限和附件分类校验后，申请合同中心区的临时上传凭证或上传地址。
- 上传完成确认：文件服务返回 `file_id`、对象标识、原始文件名、文件格式、文件大小、哈希和扫描状态；合同系统校验成功后创建附件关联记录。
- 获取预览：合同系统先校验合同、字段和附件权限，再向文件服务申请短期预览链接或预览令牌。
- 获取下载：合同系统先校验下载/导出权限，再向文件服务申请短期下载链接或下载流；不得把长期可公开访问的文件 URL 返回前端。
- 删除或归档：合同系统先校验删除权限并保留审计；文件服务按保留策略执行逻辑删除、归档或物理清理。已签署文件、审批证据和已导出快照不得被普通业务直接物理删除。
- 获取元数据：获取文件摘要、格式、大小、哈希、存储状态和病毒扫描结果。
- 文件服务负责存储层访问控制和临时链接；合同系统负责合同业务权限。文件服务不能替代合同系统判断“用户是否有权查看此合同”。

文件上传必须经过“申请上传 -> 文件服务上传 -> 上传完成确认 -> 创建附件关联”四步，避免只上传成功却没有合同关联，或合同关联已创建但文件不存在。文件删除、预览、下载、导出均需记录合同审计日志。

### 20.3 审批接口

- 创建审批实例。
- 查询审批实例。
- 撤回审批实例。
- 接收审批回调。
- 查询审批历史。

所有接口必须定义鉴权、超时、重试、幂等键、错误码和状态映射。真实服务不可用时使用 Mock 实现相同契约。

### 20.4 AI Provider 接口

- 提交合同解析任务。
- 提交法律审查任务。
- 查询任务结果。
- 接收或轮询异步结果。
- 返回 schema 版本、任务号、合同版本和结构化数据。
- 支持 Mock、Dify、直接模型 API 或其他 AI 平台的统一适配。

AppSecret 只在后端密文保存和使用，不返回前端，不写入日志。

## 21. 非功能需求

- 数据库遵循《人力资源管理系统数据库设计文档4》：BIGINT 雪花主键、snake_case、无物理外键、业务主表逻辑删除、审计字段和必要索引。
- 所有关键写操作支持幂等和乐观锁，防止重复提交和多人覆盖。
- AI、审批和文件服务采用异步任务或补偿机制，不阻塞合同核心保存。
- 合同正文、原始文件和 AI 结果必须可追溯到合同内容版本。
- 敏感字段加密或脱敏，日志和导出严格受权限控制。
- 所有状态转换必须由后端校验当前状态、权限、版本和前置条件。
- 关键操作可审计，审计记录不可被普通业务操作删除。
- 列表支持分页、组合筛选、排序和权限范围过滤。

## 22. 一期验收标准

1. 可以创建劳动、采购、销售、服务、租赁等不同类型合同，并显示不同类型字段。
2. 员工合同可以通过 HR 员工 ID 关联，不复制员工主档案；关键时点可查看快照。
3. 补充协议和变更记录必须关联原合同，不能脱离原合同创建。
4. 合同提交外部审批后业务状态仍为草稿，`approval_status` 为审批中并锁定关键字段。
5. Mock 审批服务可以覆盖提交、通过、驳回、撤回、重复回调、查询补偿和异常重试。
6. AI Provider Mock 可以完成解析和审查任务，结果与合同版本、任务号和 schema 版本关联；替换为 Dify 或其他 Provider 时不改变合同业务流程。
7. AI 失败不阻断合同保存，人工可以重发，AI 结果不能直接改变合同状态。
8. 模板、类型字段、正文、附件和合同状态均有权限和审计控制。
9. 作废合同保留查询和审计记录，但不参与有效合同统计。
10. 数据库设计通过 HR 主系统规范核对，无自增主键、物理外键或不符合命名规则的字段。

## 23. 需求评审后的产出

- 合同类型目录和字段差异矩阵
- 通用合同业务流程和状态转换表
- 外部主体、文件、审批、AI Provider 接口契约
- 角色权限矩阵
- 数据库表设计和 ER 关系说明
- 页面信息架构和功能清单
- Mock 服务约定
- 一期测试用例和验收清单

## 23.1 数据库设计依据

数据库表、字段、索引和约束统一以 `D:\project\hr-contract\数据库表\合同管理.docx` 的当前版本为准。本需求文档只描述业务需要，不维护“新增几张表”或历史增量清单；`数据库设计增量方案.md` 仅作历史设计参考，不能覆盖或替代 DOCX。

## 24. 商用页面与端到端闭环基线

### 24.0 一级导航

系统共有两个一级导航，按角色控制可见范围：

- 合同中心：合同工作台、合同台账、履行与收付款等合同业务；合同工作台即系统首页。
- 系统管理：基础配置、账号管理；仅系统管理员和配置人员可见。

普通业务用户只看到合同中心；管理员和配置人员会额外看到系统管理。合同统计不再单独设置一级导航，其只读汇总内容并入合同工作台的「经营概览」区块（管理层视角，一期先提供轻量指标、完整图表二期开放）。这样既保留配置能力，也避免把后台配置误认为日常合同业务模块，同时避免统计页与工作台重复。

### 24.0.1 菜单与功能节点的区别

功能需求中的“合同详情、合同创建、AI处理、外部审批、签署、变更、页签、按钮和校验”不是全部菜单，它们是从工作台、合同台账或合同详情进入的页面、页签和流程动作。

实际前端菜单建议为：

```text
合同中心
  工作台（= 系统首页：我的待办 / 合同概况 / 最近更新 / 经营概览）
  合同台账
    页面内合同大类选择器
    进入大类后的合同类型 Tab
  履行与收付款
    复用合同大类选择器和合同类型 Tab
系统管理（仅管理员和配置人员）
  基础配置（单页手风琴：合同分类 / 合同模板 / 外部连接 / 角色权限 / 操作日志）
  账号管理（账号 + 角色 + 数据范围默认值）
```

合同详情不是菜单，是从合同台账进入的详情页；变更与补充协议、合同级授权都收进合同详情页内作为页签/入口，不再单独设置菜单。「数据范围验收」是开发验证工具，不进导航。合同工作台即系统首页，不单独建设跨业务的独立首页；合同统计合并进工作台「经营概览」区块，只读展示合同数量与金额、生命周期、履行收付款和 AI 风险，点击结果跳转合同台账或履行台账的筛选结果，不重复实现创建、编辑、审批或履行操作。合同大类工作区属于合同台账和履行与收付款页面内的上下文选择器，不是二级菜单，也不是销售、采购、人力等独立业务系统；进入大类后，合同类型使用顶部 Tab 平铺，并按合同类型权限过滤。

### 24.1 工作台指标

工作台只保留四项核心指标卡片，另外两项作为待处理事项展示：

- 合同概况卡片：合同总数、草稿数量、待签署数量、履行中数量。
- 待处理事项：即将到期合同、待处理 AI 任务，以及待补充信息和待签署文件。

每项指标必须定义统计口径、数据权限、空数据状态和点击后的筛选条件。草稿数量不包含外部审批中的合同；审批中的合同业务状态虽然仍为草稿，但由 `approval_status` 单独展示。待处理 AI 任务只包含待人工确认、待人工修正或失败任务，不包含正常处理中任务。即将到期数量按可配置天数筛选，不单独占用核心指标卡片。

工作台只负责概览和任务入口，合同台账负责完整检索、组合筛选、批量导出和批量操作，不重复实现完整台账。

### 24.2 合同详情页面

合同详情按业务任务组织页签：概览、正文与版本、条款与履行、收付款、智能与审批、附件与变更、操作记录。空页签不显示，页面顶部只显示当前状态和当前权限允许的下一步动作。

每个动作必须具备：前置条件校验、后端状态转换、数据版本记录、失败提示、幂等控制、审计记录和成功后的下一步入口。前端不能仅根据按钮显示决定业务是否允许执行。

### 24.3 合同业务闭环

```text
创建草稿
  -> 选择类型/模板、引用主体、填写通用和类型内容
  -> 保存合同版本
  -> AI解析并人工确认
  -> 按合同类型发起法律审查
  -> 调用外部审批适配层
  -> 审批通过进入待签署，驳回或撤回回到草稿
  -> 上传签署文件并确认生效
  -> 履行中维护交付、验收和收付款
  -> 完成、终止，或从原合同发起变更/补充协议
```

每个环节必须能从合同详情进入下一环节，并能回看上一环节的版本、结果和操作日志。外部主体、文件、审批和 AI Provider 均通过适配接口接入，不能让页面直接依赖第三方返回结构。
